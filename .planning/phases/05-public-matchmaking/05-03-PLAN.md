---
phase: 05-public-matchmaking
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - test/queue.test.js
  - server.js
  - public/app.jsx
autonomous: false
requirements: [QUEUE-03]
must_haves:
  truths:
    - "When a queued player's socket disconnects, their queue entry is removed immediately"
    - "When a queued player navigates away from the queue screen, their entry is removed immediately"
    - "If a paired partner vanishes before the game starts, the surviving player is re-queued at the front of their queue and search resumes"
    - "After being alone in the queue for a delay, the player is offered a bot game (unranked, no record) but stays queued"
    - "A queue entry never lingers as a phantom pairable slot after the socket is gone"
  artifacts:
    - path: "server.js"
      provides: "disconnect-handler queue cleanup (both queues) + D-11 re-queue-at-front of surviving partner before game start"
      contains: "queues[type].delete"
    - path: "public/app.jsx"
      provides: "queue-screen useEffect cleanup emitting leaveQueue on navigate-away + delayed bot offer (D-09) reusing startBot"
      contains: "botOfferTimerRef"
    - path: "test/queue.test.js"
      provides: "QUEUE-03 unit cases live (disconnect removal, leaveQueue removal, no double-pair, re-queue at front)"
      contains: "QUEUE-03"
  key_links:
    - from: "server.js disconnect handler"
      to: "queues.casual / queues.ranked"
      via: "delete entry keyed by socket.data.clientId before the existing room cleanup"
      pattern: "queues\\[type\\]\\.delete"
    - from: "server.js disconnect handler (partner vanished, !room.started)"
      to: "queues[survivor.queueType]"
      via: "front re-insertion via new Map([[survivor], ...rest])"
      pattern: "new Map\\(\\[\\["
    - from: "server.js disconnect handler (partner vanished)"
      to: "public/app.jsx requeued handler"
      via: "io.to(survivor.socketId).emit('requeued', { type }) → client setScreen('queue') (D-11)"
      pattern: "requeued"
    - from: "public/app.jsx queue screen useEffect cleanup"
      to: "server.js leaveQueue"
      via: "socket.emit('leaveQueue') on unmount/navigate-away (D-12)"
      pattern: "leaveQueue"
---

<objective>
Deliver the queue-cleanup integrity slice (QUEUE-03): a queued player's entry is removed immediately on every phantom-slot path — socket disconnect, explicit cancel (already wired in Plan 01), and navigating away from the queue screen — so they never block a pairing slot or appear as a phantom opponent. Additionally, if a paired partner vanishes before the game actually starts, the surviving player is re-queued at the FRONT of their queue and search resumes with no penalty (D-11). Finally, after the player has been alone too long, surface the delayed "play a bot instead" offer (D-09) — an unranked client-side bot game that writes no match/rating — while keeping them queued.

This slice hardens the Plan 01/02 engine against the failure modes the QUEUE-03 success criterion calls out. It hooks the existing `socket.on("disconnect")` lifecycle and the client queue-screen lifecycle.

Purpose: No phantom slots, no dead-end mid-search on a thin pool (QUEUE-03, D-09/D-11/D-12).
Output: Disconnect/navigate-away cleanup; partner-vanish re-queue; bot offer; QUEUE-03 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-matchmaking/05-CONTEXT.md
@.planning/phases/05-public-matchmaking/05-RESEARCH.md
@.planning/phases/05-public-matchmaking/05-PATTERNS.md
@.planning/phases/05-public-matchmaking/05-UI-SPEC.md
@.planning/phases/05-public-matchmaking/05-01-SUMMARY.md
@.planning/phases/05-public-matchmaking/05-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates (exclude from source-drift verification):
- **server.js:** queue-cleanup block at the top of the existing `socket.on("disconnect")` handler; D-11 front re-queue logic for a surviving paired-but-not-started partner
- **public/app.jsx:** `socket.on("requeued", ...)` handler mapping the survivor back to the queue screen (D-11); queue-screen `useEffect` cleanup emitting `leaveQueue` on navigate-away/unmount; bot-offer firing (`botOfferTimerRef` setTimeout → `setBotOfferVisible(true)`) and the `.queue-bot-offer` render block reusing `startBot()`; i18n keys `queue.botOfferBody`, `queue.botOfferBtn`
- **public/style.css:** `.queue-bot-offer` (if not already added in Plan 01) — add only if missing
- **test/queue.test.js:** QUEUE-03 cases turned live

(Full phase symbol inventory is in Plan 01's artifacts section.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disconnect cleanup + D-11 re-queue-at-front (QUEUE-03 engine tests live)</name>
  <files>server.js, test/queue.test.js</files>
  <read_first>
    - server.js lines 1652-1665 (existing disconnect handler — exact body to extend; analog in 05-PATTERNS.md "Disconnect handler extension")
    - server.js line 769 scheduleSeatRelease (grace/seat-release lifecycle that fires when a partner vanishes)
    - server.js tryPair/createMatchedRoom/queues (Plans 01/02) — the state being cleaned
    - 05-RESEARCH.md Pattern 7 (cleanup triggers + D-11 front re-insertion via full Map replacement) + Pitfall 2 (cleanup must be FIRST action) + Pitfall 5 (Map front-insertion)
    - 05-CONTEXT.md D-11, D-12
    - test/queue.test.js QUEUE-03 todo placeholders
  </read_first>
  <behavior>
    - A clientId present in queues.casual is deleted when its socket disconnects
    - A clientId present in queues.ranked is deleted when its socket disconnects
    - Disconnect cleanup runs before the existing room cleanup and never throws when the socket was never queued
    - Partner-vanish before game start: the surviving entry is re-inserted at the FRONT of its original queue type (first in Map iteration order)
    - No double-pairing: an entry already removed by tryPair is not re-selected and is not re-queued
  </behavior>
  <action>
    In server.js, at the TOP of the existing `socket.on("disconnect")` handler (before the `const code = socket.data.code` room block), add queue cleanup: `const qClientId = socket.data.clientId || socket.id; for (const type of ["casual","ranked"]) { if (queues[type].delete(qClientId)) console.log(\`[queue] ${type} entry removed on disconnect: ${qClientId}\`); }`. For D-11, in the seat-release / disconnect path that handles a paired room: when the disconnecting socket was in a room that has NOT started (`room && room.started === false`) and the opponent is still present, build a fresh QueueEntry for the surviving opponent using their preserved `socket.data.queueType` (set at match time — adjust createMatchedRoom NOT to clear queueType until placeShips allReady; instead store the original queueType on the room or seat so it survives), then re-insert at the front of `queues[survivor.queueType]` via full Map replacement: `queues[qt] = new Map([[survivor.clientId, survivor], ...queues[qt]])` (Pitfall 5). Ensure the survivor's socket is moved back to screen "queue" by emitting a single concrete server event: `io.to(survivor.socketId).emit("requeued", { type: survivor.queueType })`. (Decision: use a dedicated `requeued` event, NOT a reused `queueStatus`/`matchFound`, so the client transition is unambiguous and independently verifiable.) Before emitting `requeued`, tear down the dead room (delete `rooms[code]`, clear its turn timer) so no phantom room lingers. Keep the survivor's enqueuedAt fresh (reset to Date.now() so their window starts narrow again — they are first in line anyway). Decision: the re-queue must NOT penalize either player (D-11). In test/queue.test.js turn the QUEUE-03 todos live (engine-level, no socket server): (a) insert an entry into queues.casual, simulate the disconnect-cleanup code path against that clientId, assert size becomes 0; (b) same for queues.ranked; (c) double-pairing: insert three casual entries, call tryPair twice in succession, assert at most one pair is formed and no entry is paired into two rooms; (d) front re-queue: given a queue with entries [X], re-insert survivor S at front, assert [...queues.casual.keys()][0] === S.clientId. If the disconnect cleanup is not directly unit-callable, extract the cleanup into a small `removeFromQueues(clientId)` helper and export it via TEST_EXPORTS so the test calls it synchronously.
  </action>
  <verify>
    <automated>npx vitest run test/queue.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - server.js disconnect handler contains `queues[type].delete(` (or `removeFromQueues(`) as its first action, iterating both "casual" and "ranked"
    - server.js contains the D-11 front re-insertion using `new Map([[` for a surviving partner when `room.started === false`
    - server.js emits `requeued` to the survivor's socket (`io.to(survivor.socketId).emit("requeued"`) after re-insertion, and tears down the dead room
    - test/queue.test.js QUEUE-03 cases (disconnect removal both queues, no double-pair, front re-queue) are live and pass
    - npm test exits 0 (full suite, no regression)
  </acceptance_criteria>
  <done>Disconnect removes the queue entry from both queues; a vanished-partner survivor is re-queued at the front; no double-pairing; QUEUE-03 engine tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Client navigate-away cleanup + delayed bot offer (D-09/D-12)</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx queue screen + botOfferTimerRef + queue-screen useEffect (added Plans 01/02) — the blocks to extend
    - public/app.jsx startBot() / single-player bot entry (the existing client-side bot reused for D-09 — confirm its call signature)
    - public/app.jsx I18N en/vi blocks
    - public/style.css .queue-bot-offer (added Plan 01 per UI-SPEC; add if missing)
    - 05-UI-SPEC.md (Bot Offer Delay, Navigate Away / Tab Close, Screen States bot-offer row, Copywriting Contract queue.botOfferBody/queue.botOfferBtn)
    - 05-CONTEXT.md D-09 (bot game is unranked, no record), D-12 (navigate-away triggers leaveQueue)
  </read_first>
  <action>
    In public/app.jsx socket `useEffect`, add the D-11 survivor handler `socket.on("requeued", ({ type }) => { setQueueType(type || "casual"); setQueueSince(Date.now()); setQueueWindow(null); setBotOfferVisible(false); setCode(null); persistRoom(null); setScreen("queue"); })` so a survivor whose partner vanished before the game started is routed straight back to the queue wait screen and a fresh search begins (matches the server `requeued` emit in Task 1). Then, in the `useEffect` gated on `screen === "queue"`: (a) on mount set `botOfferTimerRef.current = setTimeout(() => setBotOfferVisible(true), BOT_OFFER_DELAY_MS)` (use a client-side constant 30000 — mirror server BOT_OFFER_DELAY_MS value); (b) return a cleanup function that clears the bot-offer timer AND, if the player is still queued (queueType !== null and not transitioning to placement via matchFound), emits `socket.emit("leaveQueue")` so navigating away / unmount drops the entry (D-12). Guard the leaveQueue-on-cleanup so it does NOT fire on the matchFound transition (matchFound already sets queueType=null before the screen changes, so check queueType inside cleanup). In the `screen === "queue"` render, when `botOfferVisible` is true, render the `.queue-bot-offer` card per 05-UI-SPEC: a `<p className="sub">` `t("queue.botOfferBody")` and a `<button className="btn ghost">` `t("queue.botOfferBtn")` whose onClick emits `socket.emit("leaveQueue")`, resets queue state, then calls the existing `startBot()` path and routes to the bot game (the bot game is fully client-side, unranked, writes no match — do not create a server room). Add i18n keys queue.botOfferBody and queue.botOfferBtn to BOTH I18N.en and I18N.vi per the Copywriting Contract. If `.queue-bot-offer` is not present in public/style.css, add it exactly per the 05-UI-SPEC CSS block. Rebuild with esbuild.
  </action>
  <verify>
    <automated>node build-game.mjs</automated>
  </verify>
  <acceptance_criteria>
    - public/app.jsx contains `socket.on("requeued"` whose handler calls `setScreen("queue")` (routes the partner-vanish survivor back to the queue wait screen, D-11)
    - public/app.jsx queue-screen useEffect cleanup emits `socket.emit("leaveQueue")` when still queued (navigate-away/unmount), and does not fire on the matchFound transition
    - public/app.jsx renders the `.queue-bot-offer` card when botOfferVisible is true, with a button that emits leaveQueue then calls startBot()
    - public/app.jsx I18N.en AND I18N.vi both contain queue.botOfferBody and queue.botOfferBtn
    - public/style.css contains `.queue-bot-offer`
    - node build-game.mjs exits 0
  </acceptance_criteria>
  <done>A partner-vanish survivor receiving `requeued` lands back on the queue wait screen; leaving the queue screen drops the entry; after the delay a bot offer appears; accepting it starts an unranked client-side bot game without a server room; strings in EN and VI; bundle builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Queue cleanup integrity verification</name>
  <action>Pause for human verification of queue cleanup integrity. Do not proceed until the operator types "approved".</action>
  <what-built>Queue-entry cleanup on every phantom path: disconnect, navigate-away, and cancel all remove the entry; a vanished partner re-queues the survivor at the front; a delayed bot offer appears when alone.</what-built>
  <how-to-verify>
    1. `npm start`. Tab A: click Quick Match (now waiting). Tab B: open the app and click Quick Match — confirm A and B pair. Now in Tab B, before placing ships, CLOSE the tab. Confirm Tab A is returned to a searching/queue state (re-queued) rather than stuck in a dead room.
    2. Tab A in the queue: navigate away (reload or go back to lobby). Then in a Tab C, queue up and confirm Tab C does NOT get paired with the phantom A (A's entry was removed). 
    3. Queue alone in one tab and wait ~30 seconds; confirm the "Play vs Bot" offer card appears, click it, and confirm a single-player bot game starts (unranked) without a network opponent.
    4. Confirm the bot offer and its button render Vietnamese strings when language is VI.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what behaved incorrectly (phantom slot, stuck survivor, missing bot offer).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket disconnect → queue state | A dropped socket must not leave authoritative queue state behind |
| paired-but-not-started room → queue | A vanished partner must not strand the survivor or duplicate them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-10 | Denial of Service / Integrity | Phantom queue slot after disconnect | mitigate | Disconnect handler deletes the entry from BOTH queues as its FIRST action, keyed by socket.data.clientId (QUEUE-03, RESEARCH Pitfall 2) |
| T-5-11 | Integrity | Survivor double-enqueue on partner-vanish (re-queued AND still in old entry) | mitigate | Survivor's prior entry was already removed at pairing time (Plan 01 synchronous delete); re-queue creates a single fresh front entry — no duplicate |
| T-5-12 | Denial of Service | Backgrounded tab lingers as a phantom slot before socket drop | mitigate | Client queue-screen useEffect cleanup emits leaveQueue on navigate-away/unmount (D-12); socket disconnect is the backstop |
| T-5-13 | Spoofing | Bot-offer game falsely recorded as a real ranked match | mitigate | Bot game is fully client-side, creates no server room, calls no recordMatch — structurally cannot write a match/rating (D-09) |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run test/queue.test.js` — QUEUE-03 cases green (disconnect removal both queues, no double-pair, front re-queue)
- `npm test` — full suite green
- `node build-game.mjs` — client bundle builds
- Human checkpoint — partner-vanish re-queue, navigate-away cleanup (no phantom pair), delayed bot offer, EN/VI
</verification>

<success_criteria>
- A queued player's entry is removed on disconnect, cancel, and navigate-away (QUEUE-03, D-12)
- A survivor of a partner-vanish-before-start is re-queued at the front with no penalty (D-11)
- The alone-too-long bot offer appears after the delay and starts an unranked, unrecorded client-side bot game (D-09)
- No phantom pairable slot persists after the socket is gone
</success_criteria>

<output>
Create `.planning/phases/05-public-matchmaking/05-03-SUMMARY.md` when done.
</output>
