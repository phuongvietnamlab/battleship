---
phase: 19-mobile-native-app-shell-viewport-locked-single-screen-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/app.jsx
  - public/style.css
  - playwright.config.js
  - test/e2e/shell-viewport.spec.js
  - test/i18n-shell-parity.test.js
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-08, MOBILE-09, MOBILE-11]
user_setup: []

must_haves:
  truths:
    - "On any mobile viewport the page body itself never scrolls — only .shell-main may scroll"
    - "The battle screen (board + scoreboard + turn ring) fits one 100dvh viewport with no page scroll at 360×640, 390×844, 414×896"
    - "Tapping the Powers chip on the battle footer opens a bottom sheet with the power-up buttons; there is NO Log chip and NO log overlay (D-07)"
    - "The SEO/copyright footer note is reachable from the avatar menu (it is no longer a page-bottom element that requires scrolling)"
    - "On desktop (1280×800) the app still renders as a centered ~480px phone-frame column"
  artifacts:
    - path: "playwright.config.js"
      provides: "Viewport presets 360×640 / 390×844 / 414×896 + desktop 1280×800; baseURL http://localhost:4000"
      contains: "projects"
    - path: "test/e2e/shell-viewport.spec.js"
      provides: "Per-screen × per-viewport no-scroll / no-horizontal-scroll smoke assertions"
      contains: "scrollHeight"
    - path: "test/i18n-shell-parity.test.js"
      provides: "Asserts I18N.en and I18N.vi expose matching shell.* key sets"
      contains: "shell"
    - path: "public/app.jsx"
      provides: "ScreenShell wrapper, useMainHeight hook, Battle refactor (Powers sheet, no Log), shell.powersToggle i18n, footer-note relocation"
      contains: "function ScreenShell"
    - path: "public/style.css"
      provides: "Root viewport lock (100dvh/overflow:hidden), .shell-header/.shell-main/.shell-footer regions, --cell height-cap term"
      contains: ".shell-main"
  key_links:
    - from: "public/style.css :root --cell"
      to: "public/app.jsx useMainHeight --main-h"
      via: "ResizeObserver writes --main-h on .shell-main; --cell consumes it"
      pattern: "var\\(--main-h"
    - from: "public/app.jsx Battle footer Powers chip"
      to: "public/app.jsx BottomSheet"
      via: "useState open toggle renders PowerBar buttons inside BottomSheet"
      pattern: "BottomSheet"
---

<objective>
Establish the mobile-native app shell foundation and prove it on the hardest screen (battle), per D-02/D-03 shell-first sequencing.

This plan builds: the Wave 0 automated no-scroll test harness (Playwright + i18n parity), the reusable `ScreenShell` wrapper component, the `useMainHeight` ResizeObserver hook feeding a `--main-h` CSS variable, the root `100dvh`/`overflow:hidden` viewport lock, the three shell regions, the `.footer-note` relocation (which MUST land in the same plan as the root lock — Pitfall 5), and the full battle-screen refactor (scoreboard→header, boards→main sized to fit, Powers→footer-chip bottom-sheet, Log REMOVED per D-07).

Purpose: De-risk the entire phase by proving the shell pattern on the tightest viewport-fit case before refactoring the remaining 7 screens. Subsequent plans reuse `ScreenShell`, `useMainHeight`, the shell CSS regions, and the Playwright harness verbatim.

Output: Playwright config + spec, i18n parity test, ScreenShell + useMainHeight in app.jsx, shell region CSS + root lock + --cell height-cap in style.css, refactored battle screen, relocated footer-note, new shell.powersToggle EN/VI key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-UI-SPEC.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-RESEARCH.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-PATTERNS.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-CONTEXT.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test harness — Playwright config + shell-viewport spec + i18n parity test</name>
  <files>playwright.config.js, test/e2e/shell-viewport.spec.js, test/i18n-shell-parity.test.js</files>
  <read_first>
    - .planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map — this task closes all four Wave 0 gaps)
    - .planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-RESEARCH.md (Validation Architecture section: Test Framework table, Phase Requirements → Test Map, Wave 0 Gaps — note Playwright 1.60.0 already installed, no e2e dir yet)
    - .planning/config.json (workflow.app_url = http://localhost:4000 — the baseURL)
    - public/app.jsx lines 2988-4145 (App() screen-conditional render tree — to choose the screen-driving strategy)
  </read_first>
  <action>
    Create `playwright.config.js` at repo root exporting a config with `testDir: "./test/e2e"`, `baseURL: "http://localhost:4000"`, and a `projects` array defining four viewport presets: `mobile-360` (viewport 360×640), `mobile-390` (390×844), `mobile-414` (414×896), and `desktop` (1280×800). Add a `webServer` block that runs `node server.js` on port 4000 with `reuseExistingServer: true` so the suite can boot the app, OR document that the dev server must be running — prefer the `webServer` block. Do NOT add any watch/`--ui` flags.

    Create `test/e2e/shell-viewport.spec.js`. For the screen-driving strategy: implement a dev-only `?screen=` query-param hook in `App()` (Task 3 wires it) and navigate via `page.goto("/?screen=<name>")`; if a screen requires live game state (battle/placement) that cannot be reached by query param alone, drive it via the real Quick-Play→bot→placement→battle tap flow. Decide and document the chosen mechanism in a top-of-file comment. For EACH of the 8 screens (lobby, room, placement, battle, profile, history, friends, queue) × each mobile viewport project, assert the D-08 hard gate: `documentElement.scrollHeight <= innerHeight` (≤1px tolerance via `expect(scrollH).toBeLessThanOrEqual(innerH + 1)`) AND `documentElement.scrollWidth <= innerWidth + 1` (no horizontal scroll, MOBILE-11). Tag the battle and lobby cases `@smoke`. Add a separate test titled `"desktop phone frame"` (MOBILE-08) asserting that on the `desktop` project the `.app` element `getBoundingClientRect().width <= 480`. Use grep-able test titles: include the literal strings `"no page scroll"`, `"battle viewport fit"`, `"desktop phone frame"` so the VALIDATION.md commands resolve.

    Create `test/i18n-shell-parity.test.js` as a Vitest test (matches existing `test/*.test.js` convention, Vitest 4.1.8). Import or require the I18N object source-of-truth (read how I18N is exported/structured in app.jsx; if not exported, parse the keys via a regex over the file OR add a minimal export guarded by a comment). Assert that the set of keys beginning with `shell.` in `I18N.en` equals the set in `I18N.vi` (MOBILE-09). Since keys are flat dotted strings (e.g. `"shell.powersToggle"`), compare `Object.keys(I18N.en).filter(k => k.startsWith("shell."))` against the VI equivalent.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | grep -i "shell-viewport"; grep -v '^#' test/e2e/shell-viewport.spec.js | grep -c "scrollHeight"; npx vitest run test/i18n-shell-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.js` exists at repo root and `npx playwright test --list` enumerates the shell-viewport spec across the four projects (mobile-360/390/414 + desktop) without a config error.
    - `test/e2e/shell-viewport.spec.js` contains the literal grep tokens `"no page scroll"`, `"battle viewport fit"`, `"desktop phone frame"` and asserts both `scrollHeight <= innerHeight + 1` and `scrollWidth <= innerWidth + 1`.
    - `test/i18n-shell-parity.test.js` runs under `npx vitest run` and compares `shell.`-prefixed key sets across `I18N.en` and `I18N.vi` (test may initially fail/skip only if `shell.*` keys do not yet exist — Task 3 adds them; it MUST pass after Task 3).
    - No watch-mode or `--ui` flags appear in any created file.
  </acceptance_criteria>
  <done>Playwright config + shell-viewport spec + i18n parity test exist, are discoverable by the runner, and encode the D-08 hard gate (≤1px tolerance, no horizontal scroll) at the three mobile sizes plus the desktop phone-frame check.</done>
</task>

<task type="auto">
  <name>Task 2: Root viewport lock + shell region CSS + --cell height-cap (style.css)</name>
  <files>public/style.css</files>
  <read_first>
    - public/style.css lines 1-110 (:root tokens incl. --cell at line 5, html/body at line 24, body block at 26-42, .app at line 73, glass-panel rule .lobby/.room-banner/.modal/.log at 101-106)
    - public/style.css line 578 (.footer-note current rule) and lines 685-695 (.avatar-menu-item region — destination for relocated note styling)
    - .planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-RESEARCH.md (Pattern 1 ScreenShell CSS block + Pattern 2 --cell formula + Pitfall 2 min-height:0, Pitfall 5 footer-note, Pitfall 6 desktop max-width)
    - .planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-PATTERNS.md ("Root viewport lock" and ".shell-header/.shell-main/.shell-footer CSS" and "--cell height-cap formula" sections — exact analog declarations to copy)
    - .planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-UI-SPEC.md ("App Shell Layout Contract" — region padding/safe-area values: header 8px 14px + safe-area-top, footer 10px 14px + safe-area-bottom, main 14px)
  </read_first>
  <action>
    In `public/style.css`: change line 24 `html, body { height: 100%; }` to add `height: 100vh; height: 100dvh; overflow: hidden;` (declare `100vh` BEFORE `100dvh` in source order so unsupported browsers fall back). In the `body` block change `min-height: 100dvh;` (line 31) to `height: 100dvh;` and add `overflow: hidden;` — KEEP the existing `env(safe-area-inset-*)` body padding (line 28) and `overscroll-behavior: none` (line 37); do NOT duplicate them.

    Modify `.app` (line 73): keep `max-width: 480px; margin: 0 auto;` (MOBILE-08 desktop phone-frame); add `display: flex; flex-direction: column; height: 100vh; height: 100dvh;`; change `padding: 14px 14px 60px` to `padding: 0` (the 60px bottom reserve existed only for the now-relocated `.footer-note` — Pitfall 5). Add `padding-left: max(14px, env(safe-area-inset-left)); padding-right: max(14px, env(safe-area-inset-right));` per UI-SPEC landscape-notch rule, but note region gutters are applied per-region (header/main/footer each carry their own 14px) — if double-gutter results, set `.app` left/right padding to `max(0px, env(safe-area-inset-left))` instead. Verify visually no double gutter.

    Add three new region rules `.shell-header`, `.shell-main`, `.shell-footer` (place after the `.bottom-sheet-*` block ~line 277, or near `.roombar`/`.chat-panel`). Use the exact declarations from RESEARCH Pattern 1 / PATTERNS "shell CSS" section: `.shell-header { flex:none; padding:8px 14px; padding-top: calc(8px + env(safe-area-inset-top,0px)); background:var(--panel); border:1px solid var(--panel-brd); backdrop-filter:blur(14px) saturate(1.2); -webkit-backdrop-filter:blur(14px) saturate(1.2); display:flex; align-items:center; gap:8px; }`. `.shell-main { flex:1; min-height:0; overflow-y:auto; overflow-x:hidden; padding:14px; }` — `min-height:0` is MANDATORY (Pitfall 2). `.shell-footer { flex:none; padding:10px 14px; padding-bottom: calc(10px + env(safe-area-inset-bottom,0px)); background:var(--panel); border-top:1px solid var(--panel-brd); backdrop-filter:blur(14px) saturate(1.2); -webkit-backdrop-filter:blur(14px) saturate(1.2); }`. Add `max-width:100%; overflow-x:hidden;` to all three regions (MOBILE-11). Add a `.shell-header h2 { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }` rule so long titles truncate at 320px (MOBILE-11).

    Extend the `--cell` custom property (line 5) to: `--cell: min(32px, calc((100vw - 40px) / 11), calc((var(--main-h, 100vh) - 220px) / 11));`. The `220px` reserved constant is a starting estimate (RESEARCH A5) — leave a comment noting it must be tuned against the rendered scoreboard + footer-chip heights at 360×640.
  </action>
  <verify>
    <automated>grep -n "overflow: hidden" public/style.css | grep -c .; grep -c "shell-main" public/style.css; grep -c "var(--main-h" public/style.css; node build-game.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `html, body` and `body` rules use `height: 100dvh` + `overflow: hidden` (no remaining `min-height: 100dvh` on body).
    - `.shell-header`, `.shell-main`, `.shell-footer` rules exist; `.shell-main` includes BOTH `flex: 1` and `min-height: 0` (Pitfall 2 guard).
    - `--cell` declaration includes the `calc((var(--main-h, 100vh) - 220px) / 11)` height-cap term.
    - `.app` retains `max-width: 480px; margin: 0 auto;` and gains `display: flex; flex-direction: column; height: 100dvh;` with the `60px` bottom padding removed.
    - `node build-game.mjs` completes without error (CSS is static but build must still succeed end-to-end).
  </acceptance_criteria>
  <done>Root is viewport-locked, the three glass shell regions exist with safe-area insets and no-horizontal-scroll guards, and the board `--cell` formula caps by measured main height. No body-level page scroll is possible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScreenShell + useMainHeight + ?screen= hook + battle refactor + footer-note relocation + shell.powersToggle (app.jsx)</name>
  <files>public/app.jsx</files>
  <behavior>
    - shell.powersToggle key exists in both I18N.en ("⚡ Powers") and I18N.vi ("⚡ Vũ khí") → i18n-shell-parity.test.js passes.
    - Battle screen renders scoreboard in .shell-header, boards in .shell-main, a Powers footer chip in .shell-footer; NO element with className "log" remains in the Battle return (D-07).
    - Tapping the Powers chip toggles a BottomSheet (useState open) containing the PowerBar buttons.
    - shell-viewport.spec.js "battle viewport fit" passes at 360×640/390×844/414×896 (no page scroll, no horizontal scroll).
    - The footer/copyright note is reachable via a new avatar-menu "About" item, not as a page-bottom .footer-note div.
  </behavior>
  <read_first>
    - public/app.jsx lines 860-891 (BottomSheet component — the analog ScreenShell mirrors; reused verbatim for the Powers sheet)
    - public/app.jsx lines 1605 (PowerBar signature) and 1771-1856 (Battle function: scoreboard 1773-1819, boards 1820-1849, .log block at 1851-1854 to DELETE per D-07)
    - public/app.jsx lines 1759-1766 (turn-timer useEffect — effect/cleanup shape for useMainHeight)
    - public/app.jsx lines 72 (EN history.* keys) and 264 (VI history.* keys) — insertion point for shell.powersToggle
    - public/app.jsx lines 2988 (App() start), 3892-3926 (topbar/avatar menu region — destination for "About" item), 4141 (.footer-note current render to relocate), 2237-region (AvatarMenu component)
    - .planning/phases/.../19-PATTERNS.md ("ScreenShell component", "useMainHeight hook", "Battle screen refactor", "footer-note relocation", "New i18n key shell.powersToggle" sections — verbatim apply blocks with line refs)
    - .planning/phases/.../19-UI-SPEC.md (per-screen mapping table row "battle"; Overlay/Bottom-Sheet contract; D-07 note) and 19-CONTEXT.md (D-04 always-visible board+scoreboard+turn-ring; D-05 footer chips = Powers + Chat only; D-07 Log removed)
    - .planning/phases/.../19-RESEARCH.md (Pattern 2 useMainHeight verbatim; Open Question 2 footer-note relocation as avatar-menu "About" modal)
  </read_first>
  <action>
    Add `useMainHeight(ref)` hook near the top utility-hook area of `app.jsx`, copied verbatim from RESEARCH Pattern 2 (ResizeObserver writing `el.style.setProperty("--main-h", entry.contentRect.height + "px")`, with the `typeof ResizeObserver === "undefined"` → `window.resize` fallback, and `ro.disconnect()` cleanup). Follow the effect/cleanup shape of the existing turn-timer useEffect.

    Add the `ScreenShell({ header, footer, children, screenKey, direction })` component near `BottomSheet` (~line 860). It renders `{header && <div className="shell-header">{header}</div>}`, `<div className={"shell-main" + (direction ? " screen-enter-" + direction : "")} ref={mainRef} key={screenKey}>{children}</div>` (call `useMainHeight(mainRef)` on the internal ref), and `{footer && <div className="shell-footer">{footer}</div>}`. Use a `<>...</>` fragment so the regions are direct flex children of `.app`. (NOTE: the `direction`/`screenKey` props are consumed by Plan 04 transitions — wire the prop plumbing now but leave the value optional/undefined-safe.)

    Add the `shell.powersToggle` key: `"shell.powersToggle": "⚡ Powers"` into the EN I18N grouping near line 72; `"shell.powersToggle": "⚡ Vũ khí"` into the VI grouping near line 264. Do NOT add `shell.logToggle` (D-07).

    Refactor the Battle screen (1771-1856): wrap its render in `<ScreenShell header={...} footer={...}>{boardsContent}</ScreenShell>`. Pass the `.scoreboard` block (PlayerCard/TurnRing/opp-stats-popup, 1773-1819) as `header`. Pass the `.boards` block (1820-1849, including `.aim-hint`) as children. Move `<PowerBar inv={inv} aim={aim} onPower={onPower} myTurn={myTurn} />` OUT of `.board-wrap` and render its buttons inside a `<BottomSheet open={powersOpen} onClose={...} title={t("shell.powersToggle")}>`; add a footer chip button (className `btn ghost compact`, label `{t("shell.powersToggle")}`) that toggles `powersOpen` via `useState`. Per UI-SPEC Open Question 2, show the Powers chip only when `inv` has a usable power-up (match existing PowerBar conditional). The footer also keeps the existing chat toggle (D-05: Powers + Chat only) guarded by `{!vsBot && ...}` exactly as today. DELETE the `.log` block (1851-1854) entirely; if `log`/`setLog`/`addLog` become fully unused after this, remove them too — otherwise keep the state and only drop the JSX + any log footer chip (there is none — never add `shell.logToggle`).

    Relocate `.footer-note` (line 4141): remove the page-bottom `<div className="footer-note">{t("footer")}</div>` and instead add a non-destructive `.avatar-menu-item` "About" (label EN "About" / VI "Giới thiệu" — reuse existing `footer` i18n content; if no About label key exists, add `shell.about` EN+VI alongside powersToggle so the i18n parity test still passes) that opens a small modal reusing `.modal`/`.overlay` classes containing `{t("footer")}`. This relocation MUST be in THIS task because the root `overflow:hidden` lock (Task 2) makes the old page-bottom note permanently unreachable (Pitfall 5).

    Add the dev-only `?screen=` test hook: near the top of `App()`, read `new URLSearchParams(location.search).get("screen")` and, if present, initialize the `screen` state to that value (guarded behind a clearly-commented `// dev/test-only screen hook` branch). This lets Task 1's Playwright spec drive each screen by URL.
  </action>
  <verify>
    <automated>grep -c "function ScreenShell" public/app.jsx; grep -c "function useMainHeight" public/app.jsx; grep -c "shell.powersToggle" public/app.jsx; grep -v '^[[:space:]]*//' public/app.jsx | grep -c 'className="log"'; node build-game.mjs && npx vitest run test/i18n-shell-parity.test.js && npx playwright test -g "battle viewport fit"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function ScreenShell"` and `grep -c "function useMainHeight"` each return ≥1.
    - `shell.powersToggle` appears in both the EN and VI I18N objects (i18n-shell-parity.test.js passes).
    - No `className="log"` remains in the Battle return (grep over non-comment lines returns 0).
    - The Powers footer chip toggles a `BottomSheet` rendering `PowerBar` buttons; chat toggle remains `{!vsBot && ...}`-guarded (D-05).
    - `.footer-note` page-bottom div removed; an avatar-menu "About"/"Giới thiệu" item opens a modal with `{t("footer")}` content.
    - `node build-game.mjs` succeeds and `npx playwright test -g "battle viewport fit"` passes at all three mobile sizes (no page scroll, no horizontal scroll).
  </acceptance_criteria>
  <done>The ScreenShell + useMainHeight foundation exists and is proven on the battle screen: scoreboard in header, boards sized to fit in main, Powers in a footer-chip bottom sheet, Log fully removed (D-07), footer-note relocated to the avatar menu, and the dev `?screen=` hook lets the harness drive every screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a client-only CSS/JSX layout restructure. No new network endpoints, no new user-input handlers, no server changes. The only new interactive elements (Powers footer chip, "About" menu item, dev `?screen=` hook) are local UI-state toggles routed through existing render paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `?screen=` dev test hook in App() | accept | Read-only client navigation hook; sets local `screen` state only. No server trust, no privileged action gated on screen value (server remains authoritative for all game actions). Documented dev-only; acceptable as it cannot escalate beyond what tap navigation already allows. |
| T-19-02 | Information Disclosure | Long untrusted names/room codes in shell-header title | accept (UX, not security) | MOBILE-11 truncation (`text-overflow: ellipsis` on `.shell-header h2`) prevents layout-break from long names; this is a layout concern. Existing i18n/output escaping in React JSX is unchanged and preserved. |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable — zero external packages"). No supply-chain surface. |

No new authentication, session, access-control, input-validation, or cryptography surface is introduced (RESEARCH Security Domain: all ASVS categories "No").
</threat_model>

<verification>
- `node build-game.mjs` builds `dist/app.js` without error after all three tasks.
- `npx playwright test -g "battle viewport fit"` and `-g "no page scroll"` pass at 360×640/390×844/414×896.
- `npx playwright test -g "desktop phone frame"` passes (`.app` width ≤ 480px at 1280×800).
- `npx vitest run test/i18n-shell-parity.test.js` passes (shell.* EN/VI parity).
- Manual eyeball (D-08 hybrid): battle screen looks native — max board, minimal chrome, no Log block.
</verification>

<success_criteria>
- Page body never scrolls on any mobile viewport (MOBILE-01).
- Three shell regions exist and only `.shell-main` scrolls (MOBILE-02).
- Battle board + scoreboard + turn ring fit one viewport at the three mobile sizes (MOBILE-03).
- Powers is a tap-open bottom sheet; no Log chip/overlay exists (MOBILE-04 + D-07).
- Desktop preserves the centered ~480px phone-frame (MOBILE-08).
- `shell.powersToggle` present EN+VI; no untranslated strings (MOBILE-09).
- No horizontal scroll; long titles truncate (MOBILE-11).
- Playwright harness + i18n parity test exist and are green for the battle/lobby smoke set.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `ScreenShell` component (app.jsx) — `{ header, footer, children, screenKey, direction }` wrapper rendering `.shell-header`/`.shell-main`/`.shell-footer`.
- `useMainHeight(ref)` hook (app.jsx) — ResizeObserver writing the `--main-h` CSS variable on `.shell-main` (with `window.resize` fallback).
- `--main-h` CSS variable — set by `useMainHeight`, consumed by `--cell`.
- `--cell` height-cap term (style.css) — `calc((var(--main-h, 100vh) - 220px) / 11)`.
- `.shell-header` / `.shell-main` / `.shell-footer` CSS classes (style.css).
- Root viewport lock (style.css) — `html,body { height:100dvh; overflow:hidden }` + `.app` flex column.
- `shell.powersToggle` i18n key (EN "⚡ Powers" / VI "⚡ Vũ khí") and `shell.about` (if added for the About item).
- Battle Powers bottom-sheet (reuses existing `BottomSheet`); Battle `.log` block REMOVED (D-07).
- `.footer-note` relocated to an avatar-menu "About" modal.
- Dev-only `?screen=` navigation hook in `App()`.
- `playwright.config.js` (4 viewport projects).
- `test/e2e/shell-viewport.spec.js` (per-screen × per-viewport no-scroll gate).
- `test/i18n-shell-parity.test.js` (shell.* EN/VI parity).
</artifacts_produced>

<output>
Create `.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-01-SUMMARY.md` when done
</output>
