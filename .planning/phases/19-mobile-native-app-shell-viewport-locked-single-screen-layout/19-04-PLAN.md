---
phase: 19-mobile-native-app-shell-viewport-locked-single-screen-layout
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - public/app.jsx
  - public/style.css
autonomous: false
requirements: [MOBILE-06, MOBILE-07, MOBILE-10, MOBILE-12]
user_setup: []

must_haves:
  truths:
    - "Navigating between screens plays a native slide/push transition (forward and back), and under prefers-reduced-motion it degrades to a short opacity cross-fade"
    - "Opening the on-screen keyboard for the chat composer keeps the input visible above the keyboard and does not break the shell layout"
    - "Shell header clears the iPhone notch and shell footer clears the home indicator via safe-area insets"
    - "Existing behavior — reconnect/grace-window, chat bubbles, modals, install banner, offline banner — continues to work within the new shell (position:fixed overlays are not trapped inside a transformed .shell-main)"
  artifacts:
    - path: "public/app.jsx"
      provides: "navigate() direction tracking, useKeyboardInset hook wired to .chat-panel, position:fixed overlays kept as direct .app children"
      contains: "useKeyboardInset"
    - path: "public/style.css"
      provides: "screen-enter-forward/back keyframes + reduced-motion cross-fade override; final safe-area pass"
      contains: "screen-enter-forward"
  key_links:
    - from: "public/app.jsx setScreen call sites"
      to: "public/style.css .shell-main.screen-enter-* keyframes"
      via: "navigate() sets direction → ScreenShell applies screen-enter-<direction> class"
      pattern: "screen-enter"
    - from: "public/app.jsx useKeyboardInset"
      to: "public/app.jsx .chat-panel"
      via: "visualViewport.resize re-clamps chat-panel above the keyboard"
      pattern: "visualViewport"
---

<objective>
Land the cross-cutting native-feel behaviors that span all screens: tap-driven slide/push screen transitions (MOBILE-06), chat-composer keyboard handling (MOBILE-12), the final safe-area inset pass (MOBILE-07), and a behavior-preservation regression sweep ensuring `position:fixed` overlays (toast, banners, sheets, chat panel, modals) are not trapped inside a transformed `.shell-main` (MOBILE-10).

This plan runs last because transitions and the fixed-overlay audit can only be validated once all eight screens are on the shell. It is the only non-autonomous plan: it ends with an end-of-phase human verification checkpoint (config `human_verify_mode: end-of-phase`) covering the inherently manual items — real-device safe-area, keyboard behavior, transition feel, and reconnect/chat regression — per the VALIDATION.md manual-only matrix.

Purpose: Make navigation feel native and guarantee no existing flow regressed inside the new shell.

Output: navigate() + transition CSS, useKeyboardInset wired to chat-panel, final safe-area pass, fixed-overlay placement audit, and the end-of-phase human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-UI-SPEC.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-RESEARCH.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-PATTERNS.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-VALIDATION.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screen transitions — navigate() direction tracking + keyframe CSS + reduced-motion</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx lines 2988-4000 (App() setScreen call sites — dozens; pick the derive-direction-by-comparison approach per CLAUDE.md "minimal abstraction") and the ScreenShell `direction`/`screenKey` props wired in Plan 01
    - public/style.css lines 490-493 (existing prefers-reduced-motion: reduce block) and ~581 (existing prefers-reduced-motion: no-preference block) — where the new transition rules go
    - .planning/phases/.../19-RESEARCH.md (Pattern 4: CSS class-toggle transitions — 220ms slide+fade cubic-bezier(.34,1.4,.4,1), reduced-motion 100ms opacity-only; the slide-in-fwd/slide-in-back/cross-fade keyframes verbatim)
    - .planning/phases/.../19-PATTERNS.md ("navigate() direction wrapper + transition CSS" section — derive-from-comparison preferred over rewriting every setScreen call) and "No Analog Found" (screen-transition keyframes are new)
    - .planning/phases/.../19-UI-SPEC.md ("Navigation & Transitions" — slide direction rules, prefers-reduced-motion fallback, single-class-toggle implementation note)
  </read_first>
  <action>
    Determine transition direction. Define a screen-hierarchy order array (e.g. `["lobby","queue","room","placement","battle"]` plus the lobby-rooted secondary screens `["profile","history","friends"]`). Going TO `lobby` (or to a lower hierarchy index) = `"back"`; otherwise `"forward"`. Implement either: (a) a `navigate(next)` helper that computes direction from the current `screen` vs `next`, sets a `screenDirection` ref, then calls `setScreen(next)` — and route the back-navigation call sites (3987/3995/3999 `setScreen("lobby")` and the forward transitions) through it; OR (b) derive direction inside `App()` by comparing the previous screen (tracked in a ref/`usePrevious`) to the current `screen` each render. Prefer (b) per CLAUDE.md minimal-abstraction convention so existing `setScreen` call sites stay unchanged. Pass the computed `direction` and `screenKey={screen}` into each `<ScreenShell>` so it applies `screen-enter-<direction>` to `.shell-main` (the `key={screenKey}` already forces remount so the enter animation replays).

    In style.css, add (inside or beside the existing `@media (prefers-reduced-motion: no-preference)` block ~581): `.shell-main.screen-enter-forward { animation: slide-in-fwd .22s cubic-bezier(.34,1.4,.4,1) both; }` and `.shell-main.screen-enter-back { animation: slide-in-back .22s cubic-bezier(.34,1.4,.4,1) both; }`, plus `@keyframes slide-in-fwd { from { opacity:0; transform:translateX(20%);} to { opacity:1; transform:none; } }` and `slide-in-back` with `translateX(-20%)`. In the existing `@media (prefers-reduced-motion: reduce)` block (490-493), ADD a SEPARATE rule (do not merge into the `.cell`/`.boards` selector list): `.shell-main.screen-enter-forward, .shell-main.screen-enter-back { animation: cross-fade .1s linear both; }` + `@keyframes cross-fade { from { opacity:0;} to { opacity:1;} }`.

    IMPORTANT (Pitfall 3): because `.shell-main` now gets `transform` during the animation, it becomes the containing block for any descendant `position:fixed` element. Audit that `.chat-panel`, `.notice-toast`, `.offline-banner`, `.pwa-install-banner`, `.bottom-sheet-overlay`, and modal `.overlay` are rendered as DIRECT CHILDREN of `.app` (OUTSIDE `.shell-main`) — Task 2 verifies this; do not nest them inside ScreenShell children.
  </action>
  <verify>
    <automated>grep -c "screen-enter-forward" public/style.css; grep -c "slide-in-fwd" public/style.css; grep -c "cross-fade" public/style.css; node build-game.mjs && npx playwright test test/e2e/shell-viewport.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `.shell-main.screen-enter-forward` and `.screen-enter-back` rules exist under `prefers-reduced-motion: no-preference` with the 220ms `cubic-bezier(.34,1.4,.4,1)` keyframes.
    - A separate `prefers-reduced-motion: reduce` rule maps both transition classes to the 100ms `cross-fade` (not merged into the existing `.cell`/`.boards` selector).
    - `direction` + `screenKey` are passed into ScreenShell so `.shell-main` receives `screen-enter-<direction>` on navigation.
    - `node build-game.mjs` succeeds and the full `npx playwright test test/e2e/shell-viewport.spec.js` matrix passes (transitions must not reintroduce page scroll).
  </acceptance_criteria>
  <done>Screen navigation plays a 220ms slide/push (forward/back) that degrades to a 100ms cross-fade under reduced motion, with no transition-induced page scroll across the full 8-screen × 3-viewport matrix.</done>
</task>

<task type="auto">
  <name>Task 2: useKeyboardInset for chat composer + fixed-overlay placement audit + final safe-area pass</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/style.css lines 701-707 (.chat-panel: position:fixed; bottom:0; max-height:70dvh) and 716 (.chat-input input font-size:16px — MUST stay ≥16px to prevent iOS zoom)
    - public/app.jsx (ChatComposer / .chat-panel render; .notice-toast ~3940, .offline-banner ~3943, .pwa-install-banner, .bottom-sheet-overlay, modal .overlay, .footer relocated-modal — confirm all render as direct .app children at app.jsx ~3940-4141, OUTSIDE any ScreenShell .shell-main)
    - public/app.jsx lines 1759-1766 (effect/cleanup shape) for useKeyboardInset
    - .planning/phases/.../19-RESEARCH.md (Pattern 3 useKeyboardInset visualViewport reposition — graceful no-op if !window.visualViewport, MEDIUM-confidence arithmetic flagged for real-device validation; Pitfall 3 fixed-overlay containing-block trap)
    - .planning/phases/.../19-PATTERNS.md ("useKeyboardInset hook" + "Graceful no-op for optional browser APIs" shared pattern)
    - .planning/phases/.../19-UI-SPEC.md ("Keyboard / Safe-Area Handling" — header safe-area-top, footer safe-area-bottom, .app left/right safe-area, visualViewport re-clamp for chat-panel)
  </read_first>
  <action>
    Add the `useKeyboardInset(panelRef)` hook (RESEARCH Pattern 3): `const vv = window.visualViewport; if (!vv || !panelRef.current) return;` (graceful no-op per CLAUDE.md), a `reposition()` that, when `vv.height < window.innerHeight * 0.85` (keyboard-open heuristic), sets `el.style.position="fixed"; el.style.top=(vv.offsetTop + vv.height - el.offsetHeight)+"px"; el.style.bottom="auto";` and otherwise reverts (`position/top/bottom = ""`). Register `vv.addEventListener("resize"/"scroll", reposition)` with matching removal in cleanup. Wire it to the `.chat-panel` element via a ref in the ChatComposer/chat-panel render. Do NOT change `.chat-input input` font-size — it MUST stay ≥16px (iOS zoom guard, style.css:716).

    Fixed-overlay audit (Pitfall 3): confirm `.chat-panel`, `.notice-toast`, `.offline-banner`, `.pwa-install-banner`, `.bottom-sheet-overlay`, and modal `.overlay` (incl. the Plan-01 "About" modal and confirmLeave modal) are rendered as DIRECT CHILDREN of `.app`, OUTSIDE any `.shell-main`. If any currently nest inside a ScreenShell child after Plans 01-03, move them out so their `position:fixed` resolves against the viewport, not the transformed `.shell-main`. Document the audit result in the SUMMARY.

    Final safe-area pass (MOBILE-07): verify the Plan-01 `.shell-header` `padding-top: calc(8px + env(safe-area-inset-top,0px))` and `.shell-footer` `padding-bottom: calc(10px + env(safe-area-inset-bottom,0px))` are present, and that `.app` carries the left/right inset rule (`padding-left: max(14px, env(safe-area-inset-left))` etc. per UI-SPEC) without producing a double gutter against the per-region 14px. Adjust only if needed.
  </action>
  <verify>
    <automated>grep -c "useKeyboardInset" public/app.jsx; grep -c "visualViewport" public/app.jsx; grep -n "font-size:16px\|font-size: 16px" public/style.css | grep -c .; node build-game.mjs && npx playwright test test/e2e/shell-viewport.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `useKeyboardInset` exists, guards `if (!window.visualViewport) return;`, and is wired to the `.chat-panel` ref; it re-clamps the panel above the keyboard on `visualViewport.resize`/`scroll`.
    - `.chat-input input` retains `font-size:16px` (iOS zoom guard preserved — grep finds it).
    - All listed `position:fixed` overlays are direct `.app` children (outside `.shell-main`); audit documented in SUMMARY.
    - `.shell-header`/`.shell-footer` safe-area insets present; `.app` left/right inset rule present without double gutter.
    - `node build-game.mjs` succeeds and the full `shell-viewport.spec.js` matrix passes.
  </acceptance_criteria>
  <done>Chat composer stays visible above the keyboard, fixed overlays render against the viewport (not the transformed shell-main), and safe-area insets are applied on all shell edges — with the iOS 16px zoom guard preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human verification — manual-only items (MOBILE-06/07/10/12)</name>
  <what-built>
    The full mobile-native app shell across all 8 screens: viewport lock, ScreenShell regions, battle Powers sheet (no Log), room-flow + list/profile screens, slide/push transitions, chat-composer keyboard handling, and safe-area insets. Automated Playwright no-scroll gates and the i18n parity test are green.
  </what-built>
  <files>(verification only — no files modified)</files>
  <how-to-verify>
    Per VALIDATION.md "Manual-Only Verifications" (D-08 hybrid manual pass on top of the automated gate):
    1. Transitions (MOBILE-06): Navigate lobby → profile → back, lobby → friends → back, and through Quick-Play → placement → battle. Confirm a slide/push that feels native (forward slides in from right, back from left). Toggle OS "Reduce Motion" and confirm transitions become a short opacity cross-fade (no slide).
    2. Safe-area (MOBILE-07): Load on a real iPhone (notch/home-indicator) AND an Android device. Confirm the shell header clears the notch and the shell footer clears the home indicator / system nav bar — no content under the notch or behind the home bar.
    3. Keyboard (MOBILE-12): In a battle vs a human, open the chat composer and focus the input. Confirm the input stays visible directly above the on-screen keyboard (not hidden behind it) and the shell does not visually break; close the keyboard and confirm the panel returns to bottom:0.
    4. Behavior preserved (MOBILE-10): Run a full match including a reconnect (background/foreground the tab within the grace window). Confirm reconnect, chat bubbles, the install banner, the offline banner, modals (leave-confirm, About), and the opponent mini-profile popup all still render correctly above/within the shell.
    5. Confirm the battle screen has NO Log chip and NO log overlay (D-07). Confirm there is NO persistent bottom tab-bar (D-01) and NO always-visible last-shot status line (D-06).
  </how-to-verify>
  <action>Manual verification only — no code changes. The executor pauses here and presents the five manual checks above (transitions, safe-area on real devices, keyboard, behavior preservation, D-07/D-01/D-06 absence) to the developer, then records the result in the SUMMARY. All automated gates (full shell-viewport.spec.js matrix + i18n parity) must already be green before this checkpoint is presented.</action>
  <verify>
    <human-check>Developer runs the five manual checks on a real iPhone + Android and confirms all pass. Automated prerequisite: `npx playwright test test/e2e/shell-viewport.spec.js` and `npx vitest run test/i18n-shell-parity.test.js` both green.</human-check>
  </verify>
  <done>Developer types "approved" after all five manual checks pass; any regression is described and fed back as a gap.</done>
  <resume-signal>Type "approved" if all five pass, or describe any screen/flow that regressed or any safe-area/keyboard/transition issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-only: transition CSS, a `visualViewport` listener repositioning an existing fixed panel, and a fixed-overlay placement audit. No new endpoints, no new input handlers, no server changes. The chat composer input (`maxLength=200`, existing) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | `.shell-main` transform creating a containing-block trap for position:fixed overlays | mitigate | Explicit Task-2 audit keeps `.chat-panel`/`.notice-toast`/`.offline-banner`/`.pwa-install-banner`/`.bottom-sheet-overlay`/modal `.overlay` as direct `.app` children so a malformed/clipped overlay cannot hide security-relevant UI (e.g. leave-confirm modal). Prevents the transform from trapping fixed elements. |
| T-19-08 | Denial of Service | visualViewport listener leak / infinite reposition | mitigate | `useKeyboardInset` registers exactly one resize + one scroll listener with matching cleanup on unmount; graceful no-op when `visualViewport` is absent. No state write inside the listener that would re-trigger it (sets inline style on a sibling element, not the observed viewport). |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH audit: zero packages). |

No new auth/session/access-control/crypto surface. Reconnect/grace-window logic (server-side) is verified-unchanged, not modified.
</threat_model>

<verification>
- `node build-game.mjs` builds without error.
- Full `npx playwright test test/e2e/shell-viewport.spec.js` matrix (8 screens × 3 mobile viewports + desktop) green.
- `npx vitest run test/i18n-shell-parity.test.js` green.
- Human verification checkpoint (Task 3) approved.
</verification>

<success_criteria>
- Tap navigation plays native slide/push, reduced-motion → cross-fade (MOBILE-06).
- Safe-area insets clear notch + home indicator on real devices (MOBILE-07).
- All existing flows (reconnect, chat, modals, banners) work within the shell (MOBILE-10).
- Chat composer stays visible above the keyboard without breaking the shell (MOBILE-12).
- Full Playwright no-scroll matrix + i18n parity green; human verification approved.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- `navigate()` direction tracking (or usePrevious-based derivation) feeding `direction`/`screenKey` to ScreenShell.
- `screen-enter-forward` / `screen-enter-back` CSS rules + `slide-in-fwd` / `slide-in-back` / `cross-fade` keyframes (style.css), with reduced-motion override.
- `useKeyboardInset(panelRef)` hook (app.jsx) wired to `.chat-panel` via visualViewport.
- Fixed-overlay placement audit ensuring position:fixed overlays remain direct `.app` children.
- Final safe-area inset pass on shell header/footer/.app edges.
- End-of-phase human verification checkpoint result.
</artifacts_produced>

<output>
Create `.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-04-SUMMARY.md` when done
</output>
