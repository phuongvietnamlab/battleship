---
phase: 19-mobile-native-app-shell-viewport-locked-single-screen-layout
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - public/app.jsx
  - public/style.css
autonomous: true
requirements: [MOBILE-02, MOBILE-05, MOBILE-08, MOBILE-09, MOBILE-10, MOBILE-11]
user_setup: []

must_haves:
  truths:
    - "Profile renders avatar/name/stats in the shell main and the profile-actions row in the shell footer, with a back button in the header"
    - "Match History renders its header (title + back) in the shell header and its card list in the scrollable shell main; the list scrolls inside .shell-main without scrolling the page"
    - "History infinite-scroll still loads more pages as the sentinel becomes visible after the scroll container changed from the document to .shell-main"
    - "Friends renders its header in the shell header and the search + friend list in the scrollable shell main"
    - "All three screens fit / scroll correctly at 360×640, 390×844, 414×896 with no page scroll and no horizontal scroll"
  artifacts:
    - path: "public/app.jsx"
      provides: "profile/history/friends refactored to ScreenShell; IntersectionObserver root set to .shell-main"
      contains: "ScreenShell"
  key_links:
    - from: "public/app.jsx MatchHistory IntersectionObserver"
      to: "public/app.jsx .shell-main scroll container"
      via: "root option set to the shell-main ref so the sentinel triggers inside the new scroll container"
      pattern: "IntersectionObserver"
    - from: "public/app.jsx profile-actions"
      to: "public/app.jsx ScreenShell footer prop"
      via: "profile-actions row relocated to footer slot"
      pattern: "ScreenShell"
---

<objective>
Refactor the list/profile screen group — profile, match history, friends — onto the `ScreenShell` pattern.

These screens share the "header + back button + (possibly long) scrollable body" shape. History and friends are the two screens UI-SPEC explicitly allows to scroll inside `.shell-main` (their lists can exceed the viewport). This plan must also fix the Phase 13 infinite-scroll `IntersectionObserver` so its sentinel still fires once the scroll container changes from the document body to `.shell-main` (RESEARCH Open Question 1, MOBILE-10 behavior preservation).

Purpose: Bring the social/history screens under the shell while preserving Phase 13 infinite scroll and Phase 17 friends-list behavior.

Output: profile/history/friends refactored in app.jsx (incl. IntersectionObserver root fix); any scoped list CSS in style.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-UI-SPEC.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-PATTERNS.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-RESEARCH.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile + Friends screens onto ScreenShell</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx lines 2940-2965 (ProfileView: profile-view content + profile-actions block 2951-2965) and 3987 (ProfileView onBack={() => setScreen("lobby")} call site)
    - public/app.jsx lines 2396-2401 (FriendsList .friends-header: "← {t('history.back')}" back button + title) and 3999 (FriendsList render call site)
    - .planning/phases/.../19-PATTERNS.md ("Profile screen refactor" and "Friends screen refactor" sections; "Back-button + title header pattern" shared pattern — friends-header is the canonical back-button model)
    - .planning/phases/.../19-UI-SPEC.md (per-screen mapping rows "profile" and "friends": profile footer = profile-actions; friends footer = none, list may scroll)
    - .planning/phases/.../19-01-SUMMARY.md (ScreenShell signature + region classes)
  </read_first>
  <action>
    Profile: wrap `ProfileView` render in `<ScreenShell header={...} footer={...}>`. Header = title `{t("profile.title")}` (or existing profile title) + a back button `<button className="btn ghost compact" onClick={onBack}>← {t("history.back")}</button>` reusing the existing `onBack` prop. Main = the `.profile-view` content (avatar/name/stats). Footer = the existing `.profile-actions` button row (2951-2965) relocated verbatim into the footer slot — button markup unchanged. `screenKey="profile"`.

    Friends: wrap `FriendsList` render in `<ScreenShell header={...}>`. Header = the existing `.friends-header` content (the `← {t("history.back")}` back button + `👥 {t("friends.title")}` title) relocated into the header slot. Main = search input + friend list (this list may scroll inside `.shell-main` — UI-SPEC allows). No footer. `screenKey="friends"`.

    In style.css add scoped overrides only if a fit/scroll check requires it (e.g. ensure `.profile-actions` inside `.shell-footer` lays out as a row; ensure friend-list rows don't force horizontal scroll). Keep changes minimal.
  </action>
  <verify>
    <automated>node build-game.mjs && npx playwright test -g "no page scroll" --project=mobile-360 --project=mobile-390</automated>
  </verify>
  <acceptance_criteria>
    - Profile renders `.profile-view` in `.shell-main`, `.profile-actions` in `.shell-footer`, and a `history.back` back button in `.shell-header`.
    - Friends renders the `.friends-header` content in `.shell-header` and search + list in `.shell-main`.
    - `node build-game.mjs` succeeds; `npx playwright test -g "no page scroll"` passes for profile + friends at 360×640 and 390×844 (no page scroll, no horizontal scroll).
  </acceptance_criteria>
  <done>Profile and friends render inside ScreenShell with header back buttons and (profile) a footer action row, fitting/scrolling correctly at the mobile sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Match History onto ScreenShell + IntersectionObserver root fix</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx lines 2583-2609 (MatchHistory: .history-header back button + .history-title + .history-list) and 2575-2581 (sentinel IntersectionObserver — currently `{ threshold: 0.1 }` with NO root option) and 3995 (MatchHistory render call site)
    - .planning/phases/.../19-PATTERNS.md ("History screen refactor" section — IntersectionObserver root verification, history-header→header, history-list→main, line refs)
    - .planning/phases/.../19-RESEARCH.md (Open Question 1: IntersectionObserver root must be set to .shell-main ref once .shell-main, not body, is the scroll container; State of the Art table row on HIST-05)
    - .planning/phases/.../19-UI-SPEC.md (per-screen mapping row "history": scrollable list genuinely scrolls in .shell-main)
  </read_first>
  <action>
    Wrap `MatchHistory` render in `<ScreenShell header={...}>`. Header = the existing `.history-header` content (back button `← {t("history.back")}` + `.history-title` + total count) relocated into the header slot. Main = the `.history-list` card list (this scrolls inside `.shell-main`). `screenKey="history"`.

    Fix the infinite-scroll sentinel (2575-2581): the `IntersectionObserver` currently has no `root` option, so it defaulted to the document viewport. Now that `.shell-main` is the scroll container, pass `root` pointing at the `.shell-main` DOM node. Obtain the node: either (a) have `ScreenShell` forward its internal `mainRef` to children via a render-prop or `forwardRef`, or (b) query `.shell-main` (`document.querySelector(".shell-main")` or a ref) inside MatchHistory's effect and pass it as `root`. Prefer the cleanest approach that does not change ScreenShell's public API broadly — if a ref is needed, expose an optional `mainRef` callback prop on ScreenShell. Set `{ root: <shell-main node>, threshold: 0.1 }`. Guard for the node being null on first render (re-run the effect once the ref is attached).
  </action>
  <verify>
    <automated>grep -n "root:" public/app.jsx | grep -i "main\|shell"; node build-game.mjs && npx playwright test -g "no page scroll" --project=mobile-414</automated>
  </verify>
  <acceptance_criteria>
    - MatchHistory renders `.history-header` in `.shell-header` and `.history-list` in `.shell-main`.
    - The `IntersectionObserver` for the infinite-scroll sentinel passes a `root` option resolving to the `.shell-main` scroll container (grep confirms a `root:` referencing the shell-main node/ref).
    - `node build-game.mjs` succeeds; `npx playwright test -g "no page scroll" --project=mobile-414` passes for history (list scrolls inside main, page does not scroll).
  </acceptance_criteria>
  <done>Match history renders inside ScreenShell, its list scrolls within .shell-main, and infinite-scroll continues to load more pages because the IntersectionObserver root now targets the new scroll container (MOBILE-10 preserved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-only relocation of existing profile/history/friends markup into ScreenShell slots, plus an IntersectionObserver `root` change. The match-history data fetch (GET /api/matches), friend APIs, and profile stats endpoints are pre-existing and unchanged — no new request paths or input handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | long friend/opponent display names in shell-header + list rows | accept (UX, not security) | Existing `.friend-name`/`.pc-name` ellipsis truncation preserved; new `.shell-header h2` truncation (Plan 01) covers titles. Layout concern only; React JSX output escaping for user-supplied names is unchanged. |
| T-19-06 | Denial of Service | IntersectionObserver root change causing missed/duplicate page loads | mitigate | Set `root` to the actual `.shell-main` scroll container and guard against null-ref first render; verify the sentinel fires exactly once per page advance (existing `hasMore && !loading` guard at app.jsx:2577 unchanged prevents duplicate fetch storms). |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH audit: zero packages). |

No new auth/session/access-control/crypto surface. Server endpoints untouched.
</threat_model>

<verification>
- `node build-game.mjs` builds without error.
- `npx playwright test -g "no page scroll"` passes for profile, history, friends at 360×640 / 390×844 / 414×896.
- Manual eyeball (D-08 hybrid): history list scrolls inside main with header/back fixed; infinite scroll keeps loading older matches.
</verification>

<success_criteria>
- Profile, history, friends render inside ScreenShell and fit/scroll correctly at the three mobile sizes (MOBILE-05).
- Only `.shell-main` scrolls; header/footer fixed (MOBILE-02).
- Desktop phone-frame preserved (MOBILE-08).
- Back buttons reuse `history.back`; no untranslated strings (MOBILE-09).
- Phase 13 infinite scroll preserved via IntersectionObserver root fix (MOBILE-10).
- No horizontal scroll; long names truncate (MOBILE-11).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- profile screen refactored: header (back) / profile-view main / profile-actions footer.
- friends screen refactored: friends-header header / search + list main.
- history screen refactored: history-header header / history-list main.
- IntersectionObserver `root` set to the `.shell-main` scroll container (MOBILE-10 infinite-scroll fix).
- (Optional) ScreenShell `mainRef` callback prop (if needed to forward the scroll-container node to MatchHistory).
- No new i18n keys (reuses `history.back`, existing profile/history/friends keys).
</artifacts_produced>

<output>
Create `.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-03-SUMMARY.md` when done
</output>
