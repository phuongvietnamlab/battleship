---
phase: 19-mobile-native-app-shell-viewport-locked-single-screen-layout
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - public/app.jsx
  - public/style.css
autonomous: true
requirements: [MOBILE-02, MOBILE-05, MOBILE-08, MOBILE-09, MOBILE-11]
user_setup: []

must_haves:
  truths:
    - "The lobby fits one viewport: the existing topbar is the shell header, the hero CTA + cards are in the scrollable-safety main region, no page scroll"
    - "The room screen has a shell header with a back button that routes through the existing leave-room confirmation (not a silent screen pop)"
    - "The placement screen's Confirm-placement button sits in the shell footer; the board + power-up shop are in the main region; all fit one viewport"
    - "The queue screen shows the title in the header, timer in main, and Cancel in the footer; no back button (cancel is the action)"
    - "All four screens fit 360×640, 390×844, 414×896 with no page scroll and no horizontal scroll"
  artifacts:
    - path: "public/app.jsx"
      provides: "lobby/room/placement/queue screens refactored to wrap content in ScreenShell"
      contains: "ScreenShell"
  key_links:
    - from: "public/app.jsx room/placement shell-header back button"
      to: "public/app.jsx leaveRoom()"
      via: "onBack calls leaveRoom() → confirmLeave modal, not setScreen('lobby')"
      pattern: "leaveRoom"
    - from: "public/app.jsx placement Confirm button"
      to: "public/app.jsx ScreenShell footer prop"
      via: "existing confirm button relocated to ScreenShell footer slot"
      pattern: "ScreenShell"
---

<objective>
Refactor the room-flow screen group — lobby, room, placement, queue — onto the proven `ScreenShell` pattern from Plan 01.

These four screens share the create/join/place lifecycle and the leave-room confirmation flow, so grouping them keeps that behavior consistent. Each screen's existing content is relocated into ScreenShell's header/main/footer slots with minimal markup change (content moves, not rewrites), per the UI-SPEC per-screen mapping table.

Purpose: Bring the entry and match-setup screens under the viewport-locked shell without regressing Phase 9's lobby one-viewport fit (LOBBY-07) or the existing forfeit-confirmation behavior.

Output: lobby/room/placement/queue refactored in app.jsx; any per-screen compaction CSS in style.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-UI-SPEC.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-PATTERNS.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-CONTEXT.md
@.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lobby + Queue screens onto ScreenShell</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx lines 3892-3926 (topbar block: logo badge + tagline + topbar-right avatar/sound) and 3963 (Lobby component render) — topbar becomes the lobby shell header
    - public/app.jsx lines 3965-3981 (queue screen block: title h2, queue-timer, searching status-pill, handleLeaveQueue cancel button)
    - .planning/phases/.../19-PATTERNS.md ("Lobby screen refactor" and "Queue screen refactor" sections — verbatim apply blocks with line refs)
    - .planning/phases/.../19-UI-SPEC.md (per-screen mapping table rows "lobby" and "queue"; note lobby footer = none, hero CTA stays in main; queue footer = Cancel, no back button)
    - .planning/phases/.../19-01-SUMMARY.md (ScreenShell signature + shell region classes from Plan 01)
  </read_first>
  <action>
    Lobby: wrap the lobby render so the existing `.topbar` block (3892-3926) is passed as the `header` prop of `<ScreenShell>` and the `Lobby` component (3963) content is the children (rendered in `.shell-main`). No footer (UI-SPEC: lobby footer = none; hero CTA stays in main per LOBBY-07). Do NOT change topbar content — only relocate it from a top-level `.app` child into the header slot. Pass `screenKey="lobby"` for future transition keying.

    Queue: wrap the queue block (3965-3981) in `<ScreenShell header={...} footer={...}>`. Header = the queue `<h2>` title (`queue.titleWagered`/`queue.titleFree`) with NO back button (UI-SPEC: cancel is the action). Main = the `.queue-timer` + `status-pill` searching content. Footer = the `handleLeaveQueue` Cancel button (`btn ghost`). Remove the spacer `<div style={{height:20}} />` since the footer now anchors the cancel button.

    In style.css, if the topbar's existing `padding-bottom:18px` (line ~88) creates excess header height inside `.shell-header`, add a scoped override `.shell-header .topbar { padding-bottom: 0; }` (or compact as needed) — keep changes minimal and only if a 360×640 fit check requires it.
  </action>
  <verify>
    <automated>node build-game.mjs && npx playwright test -g "no page scroll" --project=mobile-360</automated>
  </verify>
  <acceptance_criteria>
    - Lobby renders the existing `.topbar` inside `.shell-header` and Lobby content inside `.shell-main`; no `.footer-note` reintroduced.
    - Queue renders title in header (no back button), timer/pill in main, Cancel button in `.shell-footer`.
    - `node build-game.mjs` succeeds.
    - `npx playwright test -g "no page scroll" --project=mobile-360` passes for lobby and queue (no page scroll, no horizontal scroll at 360×640).
  </acceptance_criteria>
  <done>Lobby and queue render inside ScreenShell, fit one viewport at 360×640, and preserve Phase 9 lobby content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Room + Placement screens onto ScreenShell with leave-confirm back button</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx lines 4004-4030 (room screen block: room.title, room-code-box, invite link, start-placement button) and 4030-region (.room-banner status/room-code)
    - public/app.jsx lines 3823-3831 (leaveRoom/doLeave/confirmLeave handlers) and 3935 (.roombar leave button — the existing onBack analog) and 4121-4134 (confirmLeave modal)
    - public/app.jsx (placement screen render — room-banner header, placement board, power-up shop row, Confirm-placement button)
    - .planning/phases/.../19-PATTERNS.md ("Room/Placement screen refactor" section — back-button leave flow to copy, Confirm→footer, line refs) and "Back-button + title header pattern" shared pattern
    - .planning/phases/.../19-UI-SPEC.md (per-screen mapping rows "room" and "placement"; Copywriting Contract: back button reuses history.back, room/placement back routes through leave.* confirmLeave modal)
    - .planning/phases/.../19-CONTEXT.md (existing confirmLeave / leave.* modal flow note in code_context)
  </read_first>
  <action>
    Room: wrap the room block (4004-4024) in `<ScreenShell header={...}>`. Header = title `{t("room.title")}` + a back button `<button className="btn ghost compact" onClick={leaveRoom}>← {t("history.back")}</button>` (reuse `history.back`; back button calls `leaveRoom()` from line 3823, NOT `setScreen("lobby")`, so the existing confirmLeave forfeit modal fires). Main = room-code box, invite link, waiting pill, and the "Start placement" button (this button may stay in main per UI-SPEC, OR move to footer following the placement Confirm pattern — planner's discretion; if no footer, omit the footer prop). `screenKey="room"`.

    Placement: wrap in `<ScreenShell header={...} footer={...}>`. Header = the `.room-banner` content (status pill / room code, compacted) + back button → `leaveRoom()`. Main = placement board + hint + power-up shop row. Footer = the existing "Confirm placement" primary button relocated into the footer slot (UI-SPEC explicit). `screenKey="placement"`.

    Confirm the back button on BOTH room and placement routes through the SAME `confirmLeave` modal (4121-4134) as the current `.roombar` leave button — do not introduce a new confirmation dialog. In style.css, add `.room-banner` compaction inside `.shell-header` only if a 360×640 fit check fails (minimal scoped override).
  </action>
  <verify>
    <automated>grep -c "onClick={leaveRoom}" public/app.jsx; node build-game.mjs && npx playwright test -g "no page scroll" --project=mobile-360 --project=mobile-414</automated>
  </verify>
  <acceptance_criteria>
    - Room and placement shell-header back buttons call `leaveRoom()` (grep finds `onClick={leaveRoom}` ≥1 occurrence for the new back buttons), routing through the existing `confirmLeave` modal — not a silent `setScreen("lobby")`.
    - Placement's "Confirm placement" button is rendered in the `.shell-footer` slot.
    - Back buttons reuse the `history.back` i18n key (no new back-button string introduced).
    - `node build-game.mjs` succeeds; `npx playwright test -g "no page scroll"` passes for room + placement at 360×640 and 414×896.
  </acceptance_criteria>
  <done>Room and placement render inside ScreenShell with leave-confirm back buttons, the Confirm button anchored in the footer, fitting one viewport at the mobile sizes while preserving forfeit-confirmation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-only relocation of existing room/lobby/placement/queue markup into ScreenShell slots. No new endpoints, no new input handlers. The room-code box, invite link, and join inputs are pre-existing and unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Spoofing/Tampering | shell-header back button on room/placement | mitigate | Back button routes through the EXISTING `leaveRoom()` → `confirmLeave` flow (server-authoritative forfeit handling unchanged); it does NOT bypass forfeit logic with a silent client-side screen pop. Preserves the current trust model. |
| T-19-04 | Information Disclosure | long room codes / player names in shell-header | accept (UX, not security) | MOBILE-11 truncation on `.shell-header h2`; layout concern only. JSX output escaping unchanged. |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (RESEARCH audit: zero packages). |

No new ASVS-relevant surface (layout relocation only; server logic untouched).
</threat_model>

<verification>
- `node build-game.mjs` builds without error.
- `npx playwright test -g "no page scroll"` passes for lobby, room, placement, queue at 360×640 / 390×844 / 414×896.
- Manual eyeball (D-08 hybrid): lobby still matches Phase 9; back buttons fire the leave-confirm modal.
</verification>

<success_criteria>
- Lobby, room, placement, queue render inside ScreenShell and fit one viewport at the three mobile sizes (MOBILE-05).
- Only `.shell-main` scrolls; header/footer are fixed (MOBILE-02).
- Desktop phone-frame preserved (MOBILE-08).
- Back buttons reuse `history.back`; no untranslated strings (MOBILE-09).
- No horizontal scroll; long room codes/names truncate (MOBILE-11).
- Leave/forfeit confirmation behavior unchanged.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- lobby screen refactored to wrap topbar (header) + Lobby content (main) in ScreenShell.
- queue screen refactored: title header / timer main / Cancel footer.
- room screen refactored: title + leave-confirm back button header / room-code main.
- placement screen refactored: room-banner + back-button header / board main / Confirm-placement footer.
- (Optional) scoped `.shell-header .topbar` / `.shell-header .room-banner` compaction CSS in style.css.
- No new i18n keys (reuses `history.back`, existing room/queue/leave keys).
</artifacts_produced>

<output>
Create `.planning/phases/19-mobile-native-app-shell-viewport-locked-single-screen-layout/19-02-SUMMARY.md` when done
</output>
