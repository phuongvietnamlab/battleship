---
phase: 03-match-recording
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - db.js
  - test/match.test.js
autonomous: true
requirements: [MATCH-01]

must_haves:
  truths:
    - "Calling recordMatch(winnerUserId, loserUserId, 'normal', mode, startedAt) with DATABASE_URL set inserts exactly one row into the matches table in a single transaction (MATCH-01)"
    - "Calling recordMatch when DATABASE_URL/PGHOST/PGDATABASE are all unset returns without throwing and writes nothing (graceful no-op, D-07)"
    - "recordMatch never throws — a DB error is caught, logged with a [match] prefix, and swallowed (D-07)"
  artifacts:
    - path: "db.js"
      provides: "recordMatch single-transaction match writer (MATCH-01)"
      exports: ["recordMatch"]
    - path: "test/match.test.js"
      provides: "now-green integration + unit tests for recordMatch"
  key_links:
    - from: "db.js recordMatch"
      to: "matches table"
      via: "module-level pool.connect → BEGIN → parameterized INSERT → COMMIT"
      pattern: "INSERT INTO matches"
    - from: "db.js recordMatch"
      to: "credentials table (guest userId fallback)"
      via: "parameterized SELECT WHERE type='guest' AND external_id=$1"
      pattern: "credentials WHERE type='guest'"
---

<objective>
Add the `recordMatch` helper to `db.js` — the single, idempotent, best-effort writer that all three server.js game-end paths (Plan 03) will call. It performs one parameterized transaction into the `matches` table, no-ops gracefully when no DB is configured, and swallows every error so it can never block or break the end-game screen (D-07).

This is the thin DB slice between the schema (Plan 01) and the wiring (Plan 03): a real INSERT proven end-to-end to a queryable `matches` row.

Purpose: MATCH-01 — every completed game writes a match record (players, winner, reason) in a single transaction.
Output: `recordMatch` export in db.js; the Plan 01 db.js-source static check goes GREEN; the recordMatch integration/unit tests go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-match-recording/03-CONTEXT.md
@.planning/phases/03-match-recording/03-RESEARCH.md
@.planning/phases/03-match-recording/03-PATTERNS.md
@db.js
@store.js
</context>

## Artifacts this phase produces

This plan adds `recordMatch` (db.js export) and the `reason` taxonomy values it accepts. The `matches` table and `test/match.test.js` come from Plan 01; `room.startedAt`, seat `userId`, `room.recorded`, and the 3 server.js call sites come from Plan 03.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recordMatch to db.js</name>
  <files>db.js</files>
  <behavior>
    - recordMatch(winnerId, loserId, reason, mode, startedAt): with a configured DB, inserts one matches row and COMMITs.
    - With DATABASE_URL && PGHOST && PGDATABASE all unset: logs `[match] ...skipping...` and returns immediately, no pool.connect, no throw.
    - On any DB error during the transaction: ROLLBACK, `console.error("[match] recordMatch failed:", e.message)`, swallow (never rethrow) — unlike linkOrPromoteAccount which rethrows.
    - mode defaults to 'classic' if falsy; startedAt defaults to new Date() if falsy (Pitfall 5: restored games may lack startedAt).
    - reason is validated against the taxonomy ['normal','timeout','disconnect','leave'] before INSERT (D-02 server-side validation); an invalid reason logs `[match]` and returns without writing.
  </behavior>
  <read_first>
    - db.js lines 22-32 (poolConfig — derive the no-op guard precisely: DATABASE_URL else PGHOST/PGDATABASE discrete vars)
    - db.js lines 75-113 (upsertGuestCredential — swallow-on-catch pattern, the model recordMatch follows: log + return, NEVER rethrow)
    - db.js lines 157-237 (linkOrPromoteAccount — pool.connect → BEGIN/COMMIT/ROLLBACK/finally release transaction shape; NOTE it rethrows at the catch — recordMatch must NOT)
    - db.js lines 68-73 (section-header comment style) and lines 489-501 (module.exports object — recordMatch appended here)
    - store.js lines 19-21, 44-51 (graceful no-op: dependency-absent check + try/catch + [store] log — mirror as [match])
    - .planning/phases/03-match-recording/03-RESEARCH.md "recordMatch helper shape (db.js)" code example (lines 373-401) and "userId resolution" (lines 426-442)
    - .planning/phases/03-match-recording/03-PATTERNS.md "db.js — add recordMatch export" section (lines 67-176)
  </read_first>
  <action>
    Add a `// ─── recordMatch ───` section comment to db.js (matching the line 68-73 style) with 2-3 lines noting it is fire-and-forget, best-effort, single-transaction, and never throws (D-07).
    Implement `async function recordMatch(winnerId, loserId, reason, mode, startedAt)`:
    1. Graceful no-op guard FIRST (guard-clause style): `if (!process.env.DATABASE_URL && !process.env.PGHOST && !process.env.PGDATABASE)` → log `[match] DATABASE_URL not set — skipping match record` and return. Derive this exact set of env vars from the poolConfig logic at db.js lines 22-32, do not invent new ones.
    2. Reason taxonomy validation (D-02): if reason is not one of 'normal','timeout','disconnect','leave', log `[match] invalid reason — skipping` and return. Never write a free-text reason.
    3. Unresolvable-seat guard (D-04): if winnerId == null || loserId == null, log `[match] unresolvable user_id — skipping` and return (matches NOT NULL FK; D-04 says skip rather than block).
    4. Transaction: use the module-level `pool` (never new Pool — Pitfall: per-request pools forbidden). pool.connect → BEGIN → parameterized `INSERT INTO matches (winner_id, loser_id, reason, mode, started_at, ended_at) VALUES ($1,$2,$3,$4,$5, now())` with bindings [winnerId, loserId, reason, mode || 'classic', startedAt || new Date()] → COMMIT. All values bound as $N — never string-concatenate clientId/reason/mode (T-03 SQL-injection mitigation).
    5. catch: ROLLBACK then `console.error("[match] recordMatch failed:", e.message)` and SWALLOW (no rethrow). finally: client.release().
    Add `recordMatch` to the module.exports object at db.js ~line 489-501. Do NOT create a new file (flat structure — CLAUDE.md). Do NOT add a resolveUserId helper here unless needed — Plan 03 resolves userIds upstream on the seat (research recommendation Option A); keep recordMatch signature taking already-resolved integer userIds.
  </action>
  <verify>
    <automated>npm test -- test/match.test.js</automated>
  </verify>
  <acceptance_criteria>
    - db.js source contains `function recordMatch` and `recordMatch` appears in module.exports
    - recordMatch uses the module-level `pool` (no `new Pool` inside the function)
    - All SQL in recordMatch uses $1..$N parameter binding (no template-literal interpolation of winnerId/loserId/reason/mode)
    - recordMatch catch block calls console.error with a `[match]` prefix and does NOT contain `throw`
    - `npm test -- test/match.test.js` exits 0; the Plan 01 db.js-source `recordMatch` static check is now GREEN
  </acceptance_criteria>
  <done>db.js exports recordMatch; it no-ops without a DB, validates reason server-side, writes one parameterized transaction with a configured DB, and never throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate recordMatch tests in test/match.test.js</name>
  <files>test/match.test.js</files>
  <behavior>
    - Integration (skipIf !DATABASE_URL): after inserting two test users, recordMatch(winnerId, loserId, 'normal', 'classic', new Date()) inserts exactly one matches row; querying matches WHERE winner_id/loser_id returns one row with reason='normal'.
    - Integration: a second recordMatch call with the SAME (winner_id, loser_id, started_at) does not create a second row (the UNIQUE constraint is exercised; recordMatch swallows the duplicate-key error without throwing).
    - Unit (always runs): recordMatch with no DB env configured resolves without throwing and inserts nothing. (Simulate by temporarily clearing DATABASE_URL/PGHOST/PGDATABASE for the call, then restore — or assert via the skipIf-inverse describe.)
    - Unit: recordMatch with an invalid reason ('cheated') resolves without throwing and inserts nothing.
  </behavior>
  <read_first>
    - test/match.test.js (the scaffold from Plan 01 — convert its it.todo placeholders into real assertions)
    - test/migrate.test.js lines 18-31 (beforeAll/afterAll DB lifecycle) and lines 50-58 (information_schema column query pattern)
    - test/db.test.js lines 44-46 (hasDatabaseUrl + skipIf)
    - .planning/phases/03-match-recording/03-VALIDATION.md Per-Task Verification Map rows 03-02-02 / 03-02-03 (the exact behaviors these tests assert)
  </read_first>
  <action>
    Replace the it.todo placeholders for recordMatch behavior with real tests. In the DB-gated describe: import recordMatch from ../db.js, insert two test users (RETURNING id), call recordMatch with reason 'normal' and a fixed startedAt, then SELECT from matches and assert exactly one row with the expected winner_id/loser_id/reason. Add an idempotency test: call recordMatch twice with the identical (winnerId, loserId, startedAt) and assert still exactly one row (UNIQUE belt-and-suspenders; recordMatch must not throw on the duplicate). Clean up inserted matches + users in afterAll.
    Add always-run unit tests (outside skipIf, or in a describe that saves/clears/restores the DB env vars): recordMatch with the env vars cleared resolves and writes nothing; recordMatch with reason 'cheated' resolves and writes nothing. Wrap any env mutation in try/finally to restore process.env so other suites are unaffected. Do NOT use watch-mode flags.
  </action>
  <verify>
    <automated>npm test -- test/match.test.js</automated>
  </verify>
  <acceptance_criteria>
    - With DATABASE_URL set: a test asserts recordMatch('normal') yields exactly one matches row with reason='normal'
    - A test asserts a duplicate (winner_id, loser_id, started_at) recordMatch call does NOT create a second row and does NOT throw
    - An always-run test asserts recordMatch no-ops (no throw, no write) when DB env vars are absent
    - An always-run test asserts recordMatch rejects an invalid reason without throwing
    - `npm test -- test/match.test.js` exits 0 (DB tests skip cleanly when DATABASE_URL unset)
    - `npm test` (full suite) exits 0 — no regression in existing suites
  </acceptance_criteria>
  <done>test/match.test.js fully exercises recordMatch (one-row insert, idempotent duplicate, DB-absent no-op, invalid-reason rejection); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server.js call site → recordMatch | recordMatch receives winner/loser/reason derived entirely from server-authoritative state; it must never accept these from a client |
| recordMatch → Postgres | all values cross into SQL; must be parameter-bound, never concatenated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | SQL injection via reason/clientId/userId in recordMatch INSERT | HIGH | mitigate | All SQL uses $1..$N parameterized binding (db.js convention); no string interpolation of any value. Acceptance criteria explicitly forbid template-literal interpolation of winnerId/loserId/reason/mode. |
| T-03-04 | Tampering | free-text / forged reason value | MEDIUM | mitigate | recordMatch validates reason against the fixed taxonomy server-side before INSERT (D-02), in addition to the DB CHECK from Plan 01 |
| T-03-05 | Denial of Service | a failing/slow DB write blocking gameplay | MEDIUM | mitigate | recordMatch swallows all errors (no rethrow) and no-ops without a DB; callers (Plan 03) invoke it fire-and-forget after gameOver emits (D-07) |
| T-03-SC | Tampering | npm installs | N/A | accept | No new packages this phase (03-RESEARCH Package Legitimacy Audit — none) |
</threat_model>

<verification>
- `npm test -- test/match.test.js` exits 0; recordMatch integration + unit tests green (DB tests skip without DATABASE_URL).
- `npm test` full suite green — no regressions.
- Manual grep confirms no `new Pool` and no string-concatenated SQL inside recordMatch.
</verification>

<success_criteria>
- recordMatch exported from db.js; writes exactly one parameterized transaction with a DB, no-ops without one, never throws (MATCH-01, D-07).
- reason taxonomy validated server-side before write (D-02).
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/03-match-recording/03-02-SUMMARY.md` when done.
</output>
