---
phase: 03-match-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/match.test.js
  - migrations/004_matches.sql
autonomous: true
requirements: [MATCH-01, MATCH-03]

must_haves:
  truths:
    - "Running `npm test -- test/match.test.js` exits 0 with the static DDL checks green (DB-gated integration tests skipped when DATABASE_URL is unset)"
    - "A `matches` table exists in Postgres after migrations run, with winner_id, loser_id, reason, mode, started_at, ended_at columns and a CHECK on reason"
  artifacts:
    - path: "migrations/004_matches.sql"
      provides: "matches table DDL (MATCH-01 source of truth for Phase 4 ratings)"
      contains: "CREATE TABLE IF NOT EXISTS matches"
    - path: "test/match.test.js"
      provides: "Nyquist test scaffold for MATCH-01 + MATCH-03 (static + DB-gated integration)"
      contains: "describe.skipIf"
  key_links:
    - from: "migrations/004_matches.sql"
      to: "db.js runMigrations lexical-sort runner"
      via: "filename 004_ picked up automatically (no runner edit)"
      pattern: "004_matches"
    - from: "test/match.test.js"
      to: "matches table via information_schema.columns"
      via: "schema column assertion after runMigrations"
      pattern: "information_schema"
---

<objective>
Lay the Nyquist test foundation and the durable schema for Phase 3. Create `migrations/004_matches.sql` (the `matches` table that every game-end path will write to) and `test/match.test.js` (the single test file every downstream task verifies against). This is Wave 0 + the migration prerequisite: no `recordMatch` helper or server.js call site can be tested until these two files exist.

Purpose: MATCH-01 requires a durable match record; the table and its CHECK-constrained `reason` column are the source of truth Phase 4 ratings will extend. The test file is the verification spine for the whole phase.
Output: `migrations/004_matches.sql`, `test/match.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-match-recording/03-CONTEXT.md
@.planning/phases/03-match-recording/03-RESEARCH.md
@.planning/phases/03-match-recording/03-PATTERNS.md
@.planning/phases/03-match-recording/03-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates the first two. Later plans add the rest.

| Artifact | Created in | Description |
|----------|-----------|-------------|
| `migrations/004_matches.sql` | **Plan 01** | `matches` table + reason CHECK + IDX_ indexes |
| `test/match.test.js` | **Plan 01** | static + DB-gated tests for MATCH-01/MATCH-03 |
| `recordMatch` (db.js export) | Plan 02 | single-transaction match writer, best-effort no-op |
| `reason` taxonomy values | Plan 02/03 | `'normal' \| 'timeout' \| 'disconnect' \| 'leave'` |
| `room.startedAt` | Plan 03 | battle-start timestamp on the room (+ Redis snapshot) |
| seat `userId` storage | Plan 03 | `room.players[clientId].userId` set at seat assignment |
| `room.recorded` flag | Plan 03 | in-memory idempotency guard (dedup) |
| 3 server.js call sites | Plan 03 | doShot win / endGameForfeit / scheduleSeatRelease + leaveRoom inline |

<tasks>

<task type="auto">
  <name>Task 1: Create migrations/004_matches.sql</name>
  <files>migrations/004_matches.sql</files>
  <read_first>
    - migrations/003_email_accounts.sql (file-header comment style line 1-4; CREATE TABLE IF NOT EXISTS + column types — SERIAL PRIMARY KEY, INTEGER NOT NULL REFERENCES users(id), TIMESTAMPTZ NOT NULL DEFAULT now())
    - migrations/002_accounts.sql (IDX_ index naming: `CREATE INDEX IF NOT EXISTS "IDX_..." ON ...`; ALTER TABLE ADD COLUMN IF NOT EXISTS additive pattern — for Phase 4 reference only)
    - .planning/phases/03-match-recording/03-RESEARCH.md "Exact DDL Recommendation: 004_matches.sql" section (lines 279-321)
    - .planning/phases/03-match-recording/03-PATTERNS.md "migrations/004_matches.sql" section (lines 22-64)
  </read_first>
  <action>
    Create migrations/004_matches.sql following the 003 file-header convention: line 1 comment naming the file + requirement IDs (MATCH-01, MATCH-03), 2-3 lines explaining it stores one row per completed 2-player server game as the source of truth for Phase 4 ratings, and a note that Phase 4 (RANK-01) will ALTER TABLE ADD COLUMN IF NOT EXISTS rating columns in a future 005 migration with no breaking change to this file.
    Define `CREATE TABLE IF NOT EXISTS matches` with columns: id SERIAL PRIMARY KEY; winner_id INTEGER NOT NULL REFERENCES users(id); loser_id INTEGER NOT NULL REFERENCES users(id); reason TEXT NOT NULL; mode TEXT NOT NULL DEFAULT 'classic'; started_at TIMESTAMPTZ NOT NULL; ended_at TIMESTAMPTZ NOT NULL DEFAULT now(); created_at TIMESTAMPTZ NOT NULL DEFAULT now().
    Add an inline named CHECK constraint `CONSTRAINT matches_reason_check CHECK (reason IN ('normal','timeout','disconnect','leave'))` — this is the DB belt-and-suspenders for the D-02 server-side reason taxonomy validation; per D-02 it does not replace server-side validation.
    Add belt-and-suspenders dedup: `CONSTRAINT matches_dedup_unique UNIQUE (winner_id, loser_id, started_at)` (backs up the in-memory room.recorded flag added in Plan 03 per D-06).
    Add three indexes with IF NOT EXISTS and IDX_ prefix: IDX_matches_winner_id ON (winner_id); IDX_matches_loser_id ON (loser_id); IDX_matches_ended_at ON (ended_at DESC).
    Do NOT add any rating columns (RANK-01 is Phase 4, deferred — out of scope). All statements IF NOT EXISTS guarded so re-running is safe.
  </action>
  <verify>
    <automated>npm test -- test/match.test.js</automated>
  </verify>
  <acceptance_criteria>
    - migrations/004_matches.sql exists
    - File contains the string `CREATE TABLE IF NOT EXISTS matches`
    - File contains `CONSTRAINT matches_reason_check` referencing 'normal','timeout','disconnect','leave'
    - File contains `IDX_matches_winner_id` and `IDX_matches_loser_id`
    - File contains a UNIQUE constraint on (winner_id, loser_id, started_at)
    - File contains NO rating/glicko column (no `rating`, no `deviation`, no `volatility`)
  </acceptance_criteria>
  <done>migrations/004_matches.sql exists with the matches DDL, reason CHECK, dedup UNIQUE, and three IDX_ indexes; no rating columns present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/match.test.js Nyquist scaffold</name>
  <files>test/match.test.js</files>
  <behavior>
    - Static: migrations/004_matches.sql exists and contains `CREATE TABLE IF NOT EXISTS matches`, the reason CHECK constraint, and IDX_matches_winner_id (always runs, no DB needed).
    - Static: db.js source text contains `recordMatch` (asserts the Plan 02 export will be present; this test goes RED now and GREEN after Plan 02 — acceptable as the verification spine).
    - DB-gated (skipIf !DATABASE_URL): after runMigrations, `matches` table exists with columns winner_id, loser_id, reason, mode, started_at, ended_at (information_schema.columns query).
    - DB-gated placeholders (skipIf): describe blocks for "recordMatch inserts one row", "recordMatch is idempotent via room.recorded", "recordMatch no-ops when DATABASE_URL absent", "disconnect reason row appears" — written as the contract Plans 02/03 must satisfy. Use `it.todo(...)` for behaviors not yet implementable so the suite stays green where infra is missing but the intent is recorded.
  </behavior>
  <read_first>
    - test/db.test.js (lines 1-13 ESM imports + __dirname shim; lines 11-18 export-shape check; lines 44-46 `const hasDatabaseUrl = !!process.env.DATABASE_URL` + describe.skipIf guard)
    - test/migrate.test.js (lines 18-31 beforeAll/afterAll import db + runMigrations + pool.end cleanup; lines 50-58 information_schema.columns column-check pattern; lines 99-114 static file-existence + readFileSync source-text assertion)
    - vitest.config.js (fileParallelism:false already set — DB-gated suites run serially)
    - .planning/phases/03-match-recording/03-PATTERNS.md "test/match.test.js" section (lines 361-443)
    - .planning/phases/03-match-recording/03-VALIDATION.md Per-Task Verification Map (the 8 task rows define exactly what this file must eventually cover)
  </read_first>
  <action>
    Create test/match.test.js using the exact ESM imports + __dirname shim from test/db.test.js. Add `const hasDatabaseUrl = !!process.env.DATABASE_URL;`.
    Static describe (always runs): assert migrations/004_matches.sql exists via fs.existsSync; readFileSync and assert it contains `CREATE TABLE IF NOT EXISTS matches`, `matches_reason_check`, and `IDX_matches_winner_id`. Assert db.js source text (readFileSync of ../db.js) contains `recordMatch` — this is the static export contract for Plan 02.
    DB-gated describe via `describe.skipIf(!hasDatabaseUrl)`: in beforeAll import ../db.js, capture pool + runMigrations, run migrations, and insert two test users rows (capture their ids for winner/loser) to satisfy the FK on matches. In afterAll DELETE the test matches rows and the test users rows then `await pool.end()`. Add a test asserting the matches table columns via information_schema.columns includes winner_id, loser_id, reason, mode, started_at, ended_at.
    Use `it.todo(...)` (not failing assertions) for the recordMatch-behavior and disconnect-row cases that depend on Plan 02/03 code, so this Wave-1 file is green except for the intentional db.js-source `recordMatch` static check (which goes green once Plan 02 lands). Do NOT use watch-mode flags. Do NOT hardcode user ids — read them back from the insert RETURNING id.
    Reuse the existing pattern of resolving rootDir and migrations path via path.join(__dirname, "..").
  </action>
  <verify>
    <automated>npm test -- test/match.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/match.test.js exists and uses ESM `import { describe, it, expect ... } from "vitest"`
    - File contains `describe.skipIf` gating DB-integration tests on DATABASE_URL
    - File contains an `information_schema.columns` query asserting matches columns
    - Static (non-DB) checks for 004_matches.sql existence + content are present and pass without a DB
    - `npm test -- test/match.test.js` exits 0 when DATABASE_URL is unset (DB tests skipped; only the db.js-source `recordMatch` static check may be RED until Plan 02 — note it in the SUMMARY as the expected RED→GREEN spine)
    - No watch-mode flags in any command
  </acceptance_criteria>
  <done>test/match.test.js exists with static DDL assertions (always run) and DB-gated integration scaffolding (skipIf !DATABASE_URL) covering the matches schema; suite runs in under ~15s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Postgres DDL | DDL runs at server boot via runMigrations; the file itself is trusted source, but its CHECK constraint is the last-line defense against a bad `reason` value reaching the table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `reason` column accepting arbitrary text | LOW | mitigate | `CONSTRAINT matches_reason_check CHECK (reason IN (...))` in DDL rejects any value outside the D-02 taxonomy even if a future code path forgets server-side validation |
| T-03-02 | Tampering | duplicate match rows from racing end paths | LOW | mitigate | `UNIQUE (winner_id, loser_id, started_at)` backs up the in-memory `room.recorded` flag (D-06); a slipped race fails the INSERT, which recordMatch swallows |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | accept | No new packages introduced this phase (per 03-RESEARCH Package Legitimacy Audit — none). No install task exists. |
</threat_model>

<verification>
- `npm test -- test/match.test.js` exits 0 (static checks green; DB tests skip without DATABASE_URL).
- With DATABASE_URL set: the matches schema column test passes after migrations.
- migrations/004_matches.sql contains no rating columns (Phase 4 scope kept out).
</verification>

<success_criteria>
- migrations/004_matches.sql exists with matches DDL, reason CHECK, dedup UNIQUE, three IDX_ indexes, no rating columns.
- test/match.test.js exists as the phase verification spine (static + DB-gated), runnable with `npm test -- test/match.test.js`.
- ROADMAP MATCH-01 schema prerequisite satisfied; MATCH-03 disconnect-row test scaffolding in place.
</success_criteria>

<output>
Create `.planning/phases/03-match-recording/03-01-SUMMARY.md` when done. Note in the SUMMARY whether the db.js-source `recordMatch` static check is RED (expected until Plan 02) or GREEN.
</output>
