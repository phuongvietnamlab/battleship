---
phase: 06-bot-difficulty-tiers
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/app.jsx
  - public/style.css
autonomous: false
requirements: [BOT-01]
must_haves:
  truths:
    - "Lobby shows a 4-button tier row (Easy/Medium/Hard/Insane) replacing the single Play vs Bot button"
    - "Tapping a tier button selects it and starts placement in one action"
    - "Last-picked tier persists in localStorage (bs_botTier) and is the default on reload; Medium on first visit / invalid value"
    - "All four tier labels render in EN and VI"
    - "Advance/power-up single-player bot path is unchanged (still defaults to Medium behavior)"
  artifacts:
    - path: "public/app.jsx"
      provides: "bot-tier-row UI, saveBotTier/loadBotTier, VALID_TIERS whitelist, bot.* i18n keys, handleBot wrapper"
      contains: "bs_botTier"
    - path: "public/style.css"
      provides: ".bot-tier-row styling"
      contains: ".bot-tier-row"
  key_links:
    - from: "Lobby tier button"
      to: "startBot(false, tier)"
      via: "onBot(tier) → handleBot wrapper"
      pattern: "onBot\\("
    - from: "loadBotTier()"
      to: "localStorage bs_botTier"
      via: "whitelist-validated read"
      pattern: "VALID_TIERS"
---

<objective>
Ship the player-facing half of the slice: replace the single "Play vs Bot" button with an inline 4-button difficulty row, persist the last pick in localStorage, and localize the labels EN/VI. Threads the selected tier into `startBot(keepScore, tier)` (built in Plan 01) so picking a button immediately starts placement with that tier's algorithm.

Implements D-04 (4-button row, one-tap start), D-05 (localStorage remembers tier, default Medium), D-06 (EN/VI labels), D-07 (classic single-player only — advance path untouched). Closes SC#1 (selector shows four options).

Purpose: makes BOT-01 reachable by a real user — without this, the Plan 01 engine is dead code defaulting to Medium.
Output: tier UI + persistence + i18n in `public/app.jsx`, styling in `public/style.css`.
</objective>

<phase_goal>
**As a** single-player, **I want to** pick a difficulty before the game starts and have my choice remembered, **so that** I can challenge myself at the right level without re-selecting every time.

(Derived from ROADMAP Phase 6 Goal + BOT-01 SC#1. Depends on the Plan 01 engine.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bot-difficulty-tiers/06-CONTEXT.md
@.planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md
@.planning/phases/06-bot-difficulty-tiers/06-PATTERNS.md
@.planning/phases/06-bot-difficulty-tiers/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: localStorage tier persistence + EN/VI i18n labels</name>
  <files>public/app.jsx</files>
  <read_first>
    - public/app.jsx lines 369-377 (clientId bootstrap + saveRoom/loadRoom — the try/catch localStorage analog)
    - public/app.jsx lines 22-153 (I18N object: en + vi blocks, dot-namespaced keys, no trailing comma on last key)
    - .planning/phases/06-bot-difficulty-tiers/06-PATTERNS.md "localStorage tier persistence" and "i18n tier labels" sections (exact saveBotTier/loadBotTier shape, VALID_TIERS, key list)
    - .planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md "i18n Additions" and "Security Domain" (whitelist validation requirement, T-06-02)
  </read_first>
  <action>
    Add module-scope `const VALID_TIERS = ["easy", "medium", "hard", "insane"];` near the other top-level constants. Add `saveBotTier(tier)` and `loadBotTier()` helpers beside saveRoom/loadRoom (lines 376-377), both wrapped in try/catch; loadBotTier reads `bs_botTier`, returns the stored value only if `VALID_TIERS.includes(stored)`, else `"medium"` (also default on catch) — this is the T-06-02 whitelist mitigation and the ASVS V5 input-validation gate. Use the `bs_` key prefix (matches bs_clientId/bs_room — Pitfall 6). Add the i18n keys `bot.easy`, `bot.medium`, `bot.hard`, `bot.insane`, `bot.selectTier` to BOTH the `en` and `vi` blocks with the exact strings from 06-RESEARCH.md "i18n Additions" (EN: Easy/Medium/Hard/Insane/Select difficulty; VI: Dễ/Trung bình/Khó/Cực khó/Chọn độ khó). Keys are additive only — do not rename existing keys; respect the no-trailing-comma-on-last-key convention.
  </action>
  <verify>
    <automated>npm run build; node -e "const s=require('fs').readFileSync('public/app.jsx','utf8'); const ok=['VALID_TIERS','bs_botTier','bot.easy','Cực khó'].every(t=>s.includes(t)); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `public/app.jsx` contains `VALID_TIERS`, `saveBotTier`, `loadBotTier`, and reads/writes `bs_botTier`.
    - loadBotTier whitelist-validates and defaults to "medium" (source assertion: VALID_TIERS.includes guard present).
    - i18n keys bot.easy/medium/hard/insane/selectTier exist in both en and vi blocks (VI includes "Cực khó").
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Tier persistence helpers and bilingual labels exist; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 4-button tier row in Lobby + thread tier into startBot</name>
  <files>public/app.jsx, public/style.css</files>
  <read_first>
    - public/app.jsx line 750 (single Play vs Bot button to replace) and lines 764-771 (mode-pick multi-button row — the structural analog)
    - public/app.jsx line 734 (Lobby prop signature — onBot is already a prop)
    - public/app.jsx line 2359 (App passes onBot={startBot}; second startBot() call at line 2397 stays default Medium)
    - public/app.jsx lines 2100-2110 (startBot(keepScore, tier) from Plan 01 — confirm signature)
    - public/style.css (find .mode-pick rule to model .bot-tier-row after)
    - .planning/phases/06-bot-difficulty-tiers/06-PATTERNS.md "Lobby 4-button tier row" + "Integration Shape" (selectedTier state default = loadBotTier(), button map, handleBot wrapper)
  </read_first>
  <action>
    In Lobby, replace the single `<button ... onClick={onBot}>{t("lobby.playBot")}</button>` (line 750) with a `<div className="bot-tier-row">` that maps over `VALID_TIERS` rendering a button per tier: label `t("bot." + tier)`, class `"btn" + (selectedTier === tier ? " primary" : " ghost")`, onClick `() => { saveBotTier(tier); setSelectedTier(tier); onBot(tier); }`. Add a `selectedTier` state in Lobby initialized to `loadBotTier()`. Add a small heading using `t("bot.selectTier")` above the row. In the App component, change `onBot={startBot}` (line 2359) to `onBot={handleBot}` and add `function handleBot(tier) { startBot(false, tier); }`. Leave the queue-offer `startBot()` call at line 2397 unchanged (no tier → defaults to Medium per D-07/queue path). Do NOT touch the mode-pick row or the advance toggle (D-07 — advance bot path stays as-is). Add a `.bot-tier-row` rule to `public/style.css` modeled on `.mode-pick` (flex row, gap, equal-width buttons). Run `npm run build`. Commit message: `feat(06-02): add bot tier selector row + persistence + i18n`.
  </action>
  <verify>
    <automated>npm run build; node -e "const s=require('fs').readFileSync('public/app.jsx','utf8'); const css=require('fs').readFileSync('public/style.css','utf8'); process.exit((s.includes('bot-tier-row')&&s.includes('handleBot')&&s.includes('onBot={handleBot}')&&css.includes('.bot-tier-row'))?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Lobby renders a `bot-tier-row` mapping VALID_TIERS to buttons; selected tier gets `primary`, others `ghost`.
    - Each button onClick persists via saveBotTier and starts the game via onBot(tier).
    - App wires `onBot={handleBot}` with `handleBot(tier) => startBot(false, tier)`; line-2397 queue-offer startBot() unchanged.
    - `.bot-tier-row` exists in public/style.css; mode-pick/advance toggle untouched.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Single bot button replaced by a working tier row that threads the chosen tier into the Plan 01 engine; selected tier visually highlighted and persisted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — selector renders, persists, and advance bot unchanged (SC#1, D-05, D-06, D-07)</name>
  <files>public/app.jsx, public/style.css</files>
  <action>
    This is a human-verify checkpoint — no code changes. Pause after Task 2 and present the verification steps below to the user; resume only on explicit approval.
  </action>
  <what-built>
    A 4-button difficulty row (Easy/Medium/Hard/Insane) replacing the single "Play vs Bot" button, with localStorage persistence (bs_botTier), EN/VI labels, and one-tap start. The advance/power-up bot path was deliberately left unchanged.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` then `npm start`; open the app in a browser at the lobby.
    2. Confirm four buttons (Easy/Medium/Hard/Insane) appear in place of the old single "Play vs Bot" button, under a "Select difficulty" heading.
    3. Toggle language to Vietnamese; confirm labels read Dễ / Trung bình / Khó / Cực khó and the heading reads "Chọn độ khó".
    4. Click "Hard" — confirm it starts placement immediately (one tap). Finish/leave, reload the page: confirm Hard is the highlighted default.
    5. In devtools, clear localStorage (or delete `bs_botTier`), reload: confirm Medium is the default highlighted tier.
    6. Set `bs_botTier` to a garbage value (e.g. "xyz") in devtools, reload: confirm it falls back to Medium (no crash).
    7. Start an Advance-mode single-player bot game (toggle Advance, play vs bot): confirm it behaves exactly as before this phase (D-07 — no tier applied, prior behavior intact).
  </how-to-verify>
  <verify>
    <human-check>Steps 1-7 above all pass; user types "approved".</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issue (wrong labels, no persistence, advance-mode regression).</resume-signal>
  <done>User has confirmed the 4-button selector renders EN/VI, persists/defaults correctly, and the advance bot path is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → tier dispatch | `bs_botTier` is untrusted persisted input read on lobby load and used to pre-select / dispatch a targeting algorithm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | bs_botTier localStorage value | mitigate | loadBotTier whitelist-validates against VALID_TIERS and defaults to "medium" on any unknown/invalid/throwing read (Task 1). botPick dispatch (Plan 01) also defaults unknown values to pickMedium — defense in depth. Human-verify step 6 exercises the garbage-value path. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: zero new dependencies). |

**No high-severity threats — purely client-side UI + one whitelist-validated localStorage string. No server, network, or account surface (D-07: classic single-player only).**
</threat_model>

<verification>
- `npm run build` succeeds.
- Source assertions: VALID_TIERS, bs_botTier, saveBotTier/loadBotTier, bot.* i18n keys (en+vi), bot-tier-row, handleBot all present.
- Human-verify checkpoint covers SC#1 (4-button render), D-05 (persistence + default), D-06 (EN/VI), D-07 (advance unchanged).
</verification>

<success_criteria>
- SC#1 met: the difficulty selector shows four options before a single-player game starts.
- D-04/D-05/D-06/D-07 satisfied: one-tap tier row, persisted+defaulted tier, bilingual labels, advance path untouched.
- BOT-01 fully reachable end-to-end: pick tier → distinct algorithm (Plan 01) → observable difference.
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files, and keys created across Plan 01 + Plan 02 (downstream-consumer manifest):

**New functions (public/app.jsx):** pickEasy, pickMedium, pickHard, pickInsane, buildDensityMap, inferAxis, saveBotTier, loadBotTier, handleBot. Modified: botPick (now dispatch shell), botShoot (hit tracking + sink reset), startBot (signature `startBot(keepScore, tier="medium")`).

**New refs (public/app.jsx):** botTierRef, botHitsRef, botRemainingRef (beside myShipsRef/botShotsRef/botQueueRef ~line 1721).

**New constant:** VALID_TIERS = ["easy","medium","hard","insane"].

**New i18n keys (en + vi):** bot.easy, bot.medium, bot.hard, bot.insane, bot.selectTier.

**New CSS class (public/style.css):** .bot-tier-row.

**New localStorage key:** bs_botTier (whitelist-validated, default "medium").

**New files:** test/bot-helpers.js (exports genFleetPure, pickEasyPure, pickMediumPure, pickHardPure, pickInsanePure, buildDensityMap), test/bot.test.js.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-bot-difficulty-tiers/06-02-SUMMARY.md` when done.
</output>
