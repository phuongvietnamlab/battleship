---
phase: 06-bot-difficulty-tiers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/bot-helpers.js
  - test/bot.test.js
  - public/app.jsx
autonomous: true
requirements: [BOT-01]
must_haves:
  truths:
    - "Easy tier fires only at unshot, in-bounds cells (never a repeat)"
    - "Medium tier behaves bit-for-bit identically to the pre-phase botPick (SC#3 anchor)"
    - "Hard tier fires the highest probability-density unshot cell and never reads myShipsRef"
    - "Insane tier (parity mask + axis lock) is honest and finishes in <= Hard average shots"
    - "Headless 200-game simulation shows avg shots easy > medium > hard >= insane (SC#2)"
    - "botPick dispatches on botTierRef.current; live app and test harness share the same algorithm logic"
  artifacts:
    - path: "test/bot-helpers.js"
      provides: "Pure-function tier algorithms + genFleetPure for headless simulation"
      exports: ["pickEasyPure", "pickMediumPure", "pickHardPure", "pickInsanePure", "genFleetPure", "buildDensityMap"]
    - path: "test/bot.test.js"
      provides: "Per-tier behavior tests + 200-game ordering assertion (SC#2, SC#3)"
      contains: "describe"
    - path: "public/app.jsx"
      provides: "pickEasy/pickMedium/pickHard/pickInsane + botPick dispatch + botTierRef/botHitsRef/botRemainingRef wiring"
      contains: "function pickHard"
  key_links:
    - from: "public/app.jsx botPick()"
      to: "pickEasy/pickMedium/pickHard/pickInsane"
      via: "dispatch on botTierRef.current"
      pattern: "botTierRef\\.current"
    - from: "test/bot.test.js"
      to: "test/bot-helpers.js"
      via: "ESM import of pure pick functions"
      pattern: "from \"./bot-helpers"
---

<objective>
Build the four distinct bot targeting algorithms (Easy/Medium/Hard/Insane) and prove their win-rate separation with a headless Vitest simulation. This is the vertical-slice engine: after this plan a bot game already targets per-tier (default Medium until the selector ships in Plan 02), and the SC#2 "observably different win rates" criterion is automatically verified.

Implements D-01 (Medium = existing botPick verbatim), D-02 (four distinct algorithms), D-03 (Insane is honest — never reads player ships). Hard/Insane density algorithms follow the spike in 06-RESEARCH.md.

Purpose: BOT-01 is satisfied by four *distinct* algorithms. The algorithms are the irreducible core; the selector UI (Plan 02) is thin glue over this engine.
Output: `test/bot-helpers.js`, `test/bot.test.js`, and the dispatch + algorithm functions inside `public/app.jsx`.
</objective>

<phase_goal>
**As a** single-player, **I want to** face a bot whose targeting strategy actually changes with difficulty, **so that** Easy is winnable and Insane is punishing-but-fair.

(Derived from ROADMAP Phase 6 Goal + BOT-01. This plan delivers the algorithm half of the slice; Plan 02 delivers the player-facing selector.)
</phase_goal>

<reconciliation_note>
06-VALIDATION.md claims "no framework present today — Wave 0 installs vitest". This is STALE/WRONG. Verified facts: `vitest ^4.1.8` is already in `package.json` devDependencies, `"test": "vitest run"` script exists, `vitest.config.js` exists (`environment: "node"`, `include: ["test/**/*.test.js"]`, `fileParallelism: false`), and 10 test files already exist under `test/`. DO NOT add a vitest install task. New `test/bot.test.js` is auto-discovered by the existing config. Follow the `test/elo.test.js` convention.
</reconciliation_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bot-difficulty-tiers/06-CONTEXT.md
@.planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md
@.planning/phases/06-bot-difficulty-tiers/06-PATTERNS.md
@.planning/phases/06-bot-difficulty-tiers/06-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing headless test scaffold (test/bot-helpers.js stubs + test/bot.test.js)</name>
  <files>test/bot-helpers.js, test/bot.test.js</files>
  <read_first>
    - test/elo.test.js (the analog: ESM, `describe/it/expect` from "vitest", `await import("../module.js")`, RED-until-implemented convention)
    - vitest.config.js (confirms environment:node, include glob, fileParallelism:false — no config change needed)
    - .planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md "Validation Architecture" section (simulateGame shape, N=200, ordering + sanity-bound assertions)
    - .planning/phases/06-bot-difficulty-tiers/06-PATTERNS.md "test/bot-helpers.js" and "test/bot.test.js" sections (exact export list, genFleetPure model, ESM/no-React rule)
    - public/app.jsx lines 297-303 (FLEET_DEF), 460-468 (key/cellsFor/inBounds), 2084-2099 (genFleet model)
  </read_first>
  <behavior>
    - Test "easy never repeats": pickEasyPure returns only keys not already in shots; over a full game no key is fired twice and all are in-bounds.
    - Test "medium regression": pickMediumPure with a fixed Math.random seed reproduces the same draw order as the legacy parity+queue logic (SC#3 anchor — deterministic sequence match).
    - Test "hard honesty": pickHardPure signature accepts only { shots, hits, queue, remaining } — no ship-set parameter is read for targeting (only for the simulator's hit resolution).
    - Test "insane <= hard": over N games avg insane shots <= avg hard shots.
    - Test "ordering (SC#2)": over N=200 games, avg(easy) > avg(medium) > avg(hard) >= avg(insane); sanity bounds avg(easy) < 130 and avg(insane) > 25.
  </behavior>
  <action>
    Create `test/bot-helpers.js` as an ESM module exporting STUB functions `genFleetPure`, `pickEasyPure`, `pickMediumPure`, `pickHardPure`, `pickInsanePure`, and `buildDensityMap`, each currently throwing `new Error("not implemented")` or returning null so the suite is RED. Duplicate constants `BOARD = 11` and `FLEET_DEF` (sizes [5,4,3,3,2]) and the primitives `key(r,c)`, `cellsFor(r,c,size,dir)`, `inBounds(cells)` locally (app.jsx monolith is not importable in Node — per PATTERNS no-analog note). genFleetPure returns an array of Sets of cell-key strings, each Set tagged with a `.size` property.
    Create `test/bot.test.js` mirroring test/elo.test.js structure: import the five pick/gen functions from "./bot-helpers.js", define a local `simulateGame(pickFn, shipSets)` that loops pick→add-to-shots→hit-detect→sink-detect (splice remaining, clear hits on sink) until all ship cells are shot, returning the turn count (safety cap 300). Write the five test blocks per the behavior list using `-t` greppable names: name them so `-t easy`, `-t medium`, `-t hard`, `-t insane`, and `-t ordering` each select the right block. Tests MUST be RED on commit (helpers are stubs). Commit message: `test(06-01): add failing bot tier simulation harness`.
    Do NOT add any vitest install task or modify package.json/vitest.config.js — both already exist.
  </action>
  <verify>
    <automated>npx vitest run test/bot.test.js 2>&1 | Select-String -Pattern "fail" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test/bot-helpers.js` exports genFleetPure, pickEasyPure, pickMediumPure, pickHardPure, pickInsanePure, buildDensityMap (stubs).
    - `test/bot.test.js` imports from "./bot-helpers.js" and contains blocks selectable by `-t easy|medium|hard|insane|ordering`.
    - `npx vitest run test/bot.test.js` exits NON-zero (RED) because helpers are unimplemented.
    - No new entry in package.json devDependencies; vitest.config.js unchanged.
  </acceptance_criteria>
  <done>Headless harness exists and is RED; the five tiered/ordering assertions are written and discoverable by the existing vitest config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the four pure algorithms in test/bot-helpers.js (turn the harness GREEN)</name>
  <files>test/bot-helpers.js</files>
  <read_first>
    - test/bot-helpers.js (the stubs from Task 1)
    - test/bot.test.js (the assertions these implementations must satisfy)
    - .planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md "The Core Algorithms — Full Specification" (full pseudocode for pickEasy, pickMedium verbatim body, buildDensityMap, pickHard, pickInsane with inferAxis) and "Common Pitfalls" 1-7
    - public/app.jsx lines 2115-2128 (legacy botPick body = pickMedium source of truth)
  </read_first>
  <behavior>
    - pickEasyPure: uniform random over unshot in-bounds cells; null when none.
    - pickMediumPure: drain queue (skip already-shot) first; else parity pool, fallback to any pool; uniform random within pool. Identical draw order to legacy botPick under the same RNG.
    - buildDensityMap: count valid placements per cell over `remaining` ship sizes only (Pitfall 1); reject any placement covering a confirmed-miss cell where miss = shots minus hits (Pitfall 2).
    - pickHardPure: drain queue choosing highest-density candidate; else global highest-density unshot cell. Never inspects ship sets for targeting (D-03).
    - pickInsanePure: queue phase applies axis-lock via inferAxis (2+ collinear hits) with full-queue fallback (Pitfall 3); hunt phase uses parity-masked density with an explicit unmasked fallback when the parity pool is exhausted (Pitfall 7). On sink the simulator already clears hits (Pitfall 4).
  </behavior>
  <action>
    Replace each stub with the implementation from 06-RESEARCH.md. pickMediumPure must be a faithful de-ref'd copy of the legacy `botPick` body (app.jsx:2115-2128) so the SC#3 regression assertion passes. Implement `buildDensityMap({ shots, hits, remaining })` filtering miss cells and iterating only `remaining` sizes. Implement `inferAxis` from the bot's own hit geometry (split keys, compare rows/cols) — it must read ONLY the passed `hits` set, never a ship set (D-03 honesty boundary). Add the parity-mask + unmasked fallback in pickInsanePure's hunt phase. Run the suite; iterate until all five blocks are GREEN including the N=200 ordering assertion. Commit message: `feat(06-01): implement easy/medium/hard/insane pure targeting algorithms`.
  </action>
  <verify>
    <automated>npx vitest run test/bot.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/bot.test.js` exits 0 with all blocks green.
    - `npx vitest run test/bot.test.js -t ordering` green: avg(easy) > avg(medium) > avg(hard) >= avg(insane), avg(easy) < 130, avg(insane) > 25.
    - `test/bot-helpers.js` contains `function buildDensityMap` and `inferAxis`; neither references any player ship set (grep: no `myShips` token in the file).
    - pickMediumPure reproduces legacy botPick draw order (the `-t medium` regression block is green).
  </acceptance_criteria>
  <done>All four algorithms implemented as pure functions; SC#2 ordering and SC#3 Medium-regression proven green by the headless harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port algorithms + dispatch into public/app.jsx (botPick, botShoot, startBot, new refs)</name>
  <files>public/app.jsx</files>
  <read_first>
    - public/app.jsx lines 1721-1723 (existing refs myShipsRef/botShotsRef/botQueueRef — add new refs here)
    - public/app.jsx lines 2100-2110 (startBot reset block — extend signature + init new refs)
    - public/app.jsx lines 2115-2128 (botPick body → becomes pickMedium verbatim)
    - public/app.jsx lines 2129-2161 (botShoot — add botHitsRef tracking + sink reset)
    - public/app.jsx lines 297-303, 460-468, 2084-2099 (FLEET_DEF, key/cellsFor/inBounds, genFleet — reused by density)
    - test/bot-helpers.js (the now-green algorithms to port — keep logic identical, re-ref to botShotsRef/botHitsRef/botQueueRef/botRemainingRef)
    - .planning/phases/06-bot-difficulty-tiers/06-RESEARCH.md "Integration Shape" (new refs table, botShoot hit/sink edits, botPick dispatch)
  </read_first>
  <behavior>
    - botPick() dispatches: tier "easy"→pickEasy, "hard"→pickHard, "insane"→pickInsane, else pickMedium (default covers "medium" and any unknown stored value).
    - Default tier is "medium" until Plan 02 wires the selector — existing single-player behavior is unchanged (SC#3).
    - botShoot records hits into botHitsRef; on sink it splices botRemainingRef, clears botHitsRef and botQueueRef.
  </behavior>
  <action>
    Add three useRef declarations beside line 1721-1723: `botTierRef` (init "medium"), `botHitsRef` (init new Set()), `botRemainingRef` (init []). Move the current `botPick()` body verbatim into a new `pickMedium()` (Pitfall 5 — do not alter it). Add `pickEasy()`, `pickHard()`, `pickInsane()`, and the shared `buildDensityMap()` (and `inferAxis` helper) ported from test/bot-helpers.js but reading the React refs (`botShotsRef.current`, `botHitsRef.current`, `botQueueRef.current`, `botRemainingRef.current`) instead of plain-object params. Replace `botPick()` with the guard-clause dispatch on `botTierRef.current` per PATTERNS.md. In `botShoot()` add `botHitsRef.current.add(k)` right after `botShotsRef.current.add(k)` when hit; in the sink branch splice the sunk size out of botRemainingRef and reset botHitsRef/botQueueRef. Extend `startBot(keepScore)` to `startBot(keepScore, tier = "medium")` and inside the reset block set `botTierRef.current = tier`, `botHitsRef.current = new Set()`, `botRemainingRef.current = FLEET_DEF.map(f => f.size)`. Do NOT change the `onBot={startBot}` wiring or Lobby yet (Plan 02). The density pick functions must reference ONLY bot-side refs — never `myShipsRef` for targeting (D-03; grep gate below). Run `npm run build` to confirm the JSX still bundles. Commit message: `feat(06-01): wire tier dispatch + density refs into app.jsx botPick/botShoot/startBot`.
  </action>
  <verify>
    <automated>npm run build; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `public/app.jsx` contains `function pickEasy`, `function pickMedium`, `function pickHard`, `function pickInsane`, and `botTierRef.current` dispatch in `botPick`.
    - `botHitsRef`, `botRemainingRef`, `botTierRef` declared as refs; `startBot` signature is `startBot(keepScore, tier = "medium")`.
    - D-03 grep gate: no occurrence of `myShipsRef` inside the bodies of pickHard/pickInsane/buildDensityMap (verify by reading those function bodies — only botShoot's existing hit-detection line may reference myShipsRef).
    - `npm run build` succeeds (app.jsx bundles); `npx vitest run` full suite green (no regression in the other 10 test files).
  </acceptance_criteria>
  <done>The live bot dispatches on tier and tracks hits/remaining for density; Medium path unchanged; full test suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → algorithm dispatch | The persisted `bs_botTier` string is untrusted input read on load; it selects which targeting function runs (handled fully in Plan 02 — validated there). |
| bot algorithm → player ship data | D-03 honesty boundary: targeting functions must never read the player's actual ship cells (`myShipsRef`); only observed hit/miss evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | pickHard / pickInsane / buildDensityMap | mitigate | Architectural: density/axis logic reads only botShotsRef, botHitsRef, botRemainingRef. Task 3 acceptance includes a grep gate asserting no `myShipsRef` inside these function bodies (D-03 anti-cheat). |
| T-06-02 | Tampering | bs_botTier localStorage value | mitigate | Whitelist-validate on read against ["easy","medium","hard","insane"], default "medium". Dispatch already defaults unknown values to pickMedium (defense in depth). Full read-path validation lands in Plan 02. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase — RESEARCH.md Package Legitimacy Audit confirms zero new dependencies. No supply-chain surface. |

**No high-severity threats — client-side game logic only, no server, no network, no persistence beyond one whitelist-validated localStorage string.**
</threat_model>

<verification>
- `npx vitest run test/bot.test.js` green (all five tiered/ordering blocks).
- `npx vitest run` full suite green (no regression in existing 10 test files).
- `npm run build` succeeds.
- D-03 grep gate: pickHard/pickInsane/buildDensityMap contain no `myShipsRef` reference.
</verification>

<success_criteria>
- Four distinct targeting algorithms exist as pure functions (helpers) and as live app.jsx functions sharing identical logic (BOT-01).
- SC#2 proven automatically: avg shots easy > medium > hard >= insane over 200 games.
- SC#3 proven: pickMedium reproduces legacy botPick; default tier stays "medium" so pre-phase single-player is unchanged.
- D-03 honesty enforced by grep gate.
</success_criteria>

<output>
Create `.planning/phases/06-bot-difficulty-tiers/06-01-SUMMARY.md` when done.
</output>
