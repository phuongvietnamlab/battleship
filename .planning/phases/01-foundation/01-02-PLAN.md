---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - server.js
  - test/ratelimit.test.js
autonomous: false
requirements: [SEC-01]
user_setup: []

must_haves:
  truths:
    - "fire and useAbility socket events are rate-limited per player; an attacker sending rapid-fire events receives RATE_LIMITED errors, not a crash"
    - "chat is rate-limited per player (5 per 10s)"
    - "After repeated rapid violations the offending socket is disconnected"
    - "A simultaneous fire + turn-timeout cannot both resolve the same shot (turn-clock race guarded)"
  artifacts:
    - path: "server.js"
      provides: "fireLimiter/abilityLimiter/chatLimiter + RATE_LIMITED responses + room.resolving race guard"
      contains: "RateLimiterMemory"
  key_links:
    - from: "fire/useAbility/chat handlers"
      to: "rate-limiter-flexible limiters"
      via: "await limiter.consume(clientId) at top of handler"
      pattern: "\\.consume\\("
    - from: "fire handler + onTurnTimeout"
      to: "room.resolving flag"
      via: "set before doShot, checked in onTurnTimeout"
      pattern: "resolving"
---

<objective>
Close the most critical pre-public-matchmaking attack vector: per-player rate limiting on the `fire`, `useAbility`, and `chat` socket events (D-06, D-07, D-08), plus the turn-clock race guard (D-09) that ships in the same slice. A scripted client emitting events at flood rate now receives a structured `RATE_LIMITED` error and is dropped — and disconnected after repeated abuse — instead of freezing the opponent or exploiting the simultaneous fire+timeout race.

This is a complete vertical hardening slice: after it lands, the rapid-fire DoS vector (PITFALLS Pitfall 9 / CONCERNS #2 + #7) is closed end-to-end with an automated test proving it. Delivers SEC-01.

Purpose: Public matchmaking removes the invite-only trust assumption. Rate limiting and the turn-clock race guard must land before that queue opens (PITFALLS: "Foundation — must ship before public matchmaking").
Output: `rate-limiter-flexible` added to package.json; limiter instances + handler guards + race guard in server.js; ratelimit integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<package_legitimacy>
This plan adds one npm dependency. No RESEARCH.md audit table exists for this phase, so it is treated as `[ASSUMED]` and gated by a blocking human checkpoint (Task 0) before install.

- `rate-limiter-flexible` — the limiter library named in D-06 and PITFALLS Pitfall 9. Verify at https://www.npmjs.com/package/rate-limiter-flexible (publisher: animir; widely used for Socket.IO/Express limiting).
</package_legitimacy>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify rate-limiter-flexible legitimacy before install</name>
  <files>package.json (install gate only — no file written in this task)</files>
  <action>No files are written in this task. This is a blocking-human gate: present https://www.npmjs.com/package/rate-limiter-flexible for verification before npm install runs in Task 1. Do not proceed to install until the human types approved.</action>
  <what-built>Nothing yet — gate precedes `npm install rate-limiter-flexible`. No RESEARCH.md legitimacy audit exists, so the package is [ASSUMED] (threat T-02-SC).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/rate-limiter-flexible — confirm publisher `animir`, actively maintained, hundreds of thousands+ weekly downloads, used for rate limiting in Node/Socket.IO.
    2. Confirm exact name `rate-limiter-flexible` (no typosquat).
  </how-to-verify>
  <resume-signal>Type "approved" to allow install, or name a substitute.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Per-player limiters on fire / useAbility / chat</name>
  <files>package.json, server.js, test/ratelimit.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md §"1. Rate-limiter wrapper" — limiter instantiation block, guard pattern, structured error-code shape
    - server.js lines 54–68 (config-constant declarations — GRACE_MS/SNAPSHOT_MS — where the limiter consts go)
    - server.js lines 721–740 (fire handler), 743–806 (useAbility handler), 810–825 (chat handler) — exact handlers to wrap; note these are currently synchronous callback handlers
    - .planning/phases/01-foundation/01-CONTEXT.md D-06, D-07, D-08 (RateLimiterMemory, limit values, RATE_LIMITED code, drop + disconnect-on-repeat)
    - .planning/research/PITFALLS.md §"Pitfall 9" (limit values, server-side only)
  </read_first>
  <behavior>
    - Test (test/ratelimit.test.js): consuming the fire limiter more than 2 times within 1s for one key rejects the 3rd attempt; the handler responds `{ ok:false, code:"RATE_LIMITED" }` and does not throw.
    - Test: useAbility limiter rejects the 2nd attempt within 1s (1/s).
    - Test: chat limiter rejects the 6th attempt within 10s (5/10s).
    - Test: a key under the limit is allowed through (limiter does not block normal play).
  </behavior>
  <action>
    Add dependency `rate-limiter-flexible` to package.json (D-06). Near the server.js config constants (lines 54–68, alongside GRACE_MS), instantiate three `RateLimiterMemory` instances per D-07: `fireLimiter` `{ points: 2, duration: 1 }`, `abilityLimiter` `{ points: 1, duration: 1 }`, `chatLimiter` `{ points: 5, duration: 10 }`. Key per player by `socket.data.clientId || socket.id` (D-06). Use `RateLimiterMemory` (in-process) — Redis store is explicitly deferred to Phase 5 scaling (do NOT wire Redis here).

    At the TOP of each of the three handlers (`fire`, `useAbility`, `chat`), before any room/state access, add a guard-clause limiter check. Convert each handler to `async` and `try { await <limiter>.consume(key); } catch { cb && cb({ ok:false, code:"RATE_LIMITED" }); ...disconnect logic...; return; }` (per PATTERNS guard pattern). On violation: emit the named `RATE_LIMITED` code and DROP the event (return early — never crash, D-08). Track consecutive violations per socket (e.g. `socket.data.rlHits`); after a threshold of repeated rapid violations (choose a small constant, e.g. 10), call `socket.disconnect(true)` (D-08). Reset the counter on a successful (non-rejected) consume.

    `RATE_LIMITED` follows the existing `{ ok:false, code:"SCREAMING_SNAKE" }` convention (server.js lines 595/623/625). Server-side only — do not add client throttle (trivially bypassed, PITFALLS Pitfall 9).
  </action>
  <verify>
    <automated>npm test -- test/ratelimit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The 3rd fire within 1s for one clientId yields `{ ok:false, code:"RATE_LIMITED" }` and the process does not throw (asserted in test/ratelimit.test.js).
    - useAbility rejects at 2/s, chat rejects at 6/10s — asserted with the exact D-07 limits.
    - `grep -c "RateLimiterMemory" server.js` returns at least 3 (one per limiter), and the limiters use points/duration of {2,1},{1,1},{5,10}.
    - `grep -n "RATE_LIMITED" server.js` shows the code returned in the fire, useAbility, and chat handlers.
    - Repeated violations call `socket.disconnect` — `grep -n "disconnect(true)" server.js` shows the abuse-disconnect path.
  </acceptance_criteria>
  <done>fire/useAbility/chat are per-player rate-limited with the D-07 limits; violations drop the event with RATE_LIMITED and repeat abuse disconnects the socket; normal play is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn-clock race guard (room.resolving)</name>
  <files>server.js, test/ratelimit.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md §"3. Turn-clock race guard" — the room.resolving set/check/clear pattern and exact insertion points
    - server.js lines 444–451 (armTurnTimer), 468–482 (onTurnTimeout) — where the timeout fires
    - server.js lines 721–740 (fire handler — where doShot is called and resolving must be set/cleared)
    - .planning/phases/01-foundation/01-CONTEXT.md D-09 (turn.resolving boolean; simultaneous fire + timeout must not both resolve)
    - .planning/research/PITFALLS.md §"Pitfall 9" turn-clock corollary; .planning/codebase/CONCERNS.md #7
  </read_first>
  <behavior>
    - Test (test/ratelimit.test.js, extended) OR a focused unit: while a shot is resolving (room.resolving === true), a re-entrant fire on the same room returns `{ ok:false, code:"BAD_STATE" }` and does not call doShot a second time.
    - Test: onTurnTimeout is a no-op (does not change turn / award forfeit) when room.resolving is true.
  </behavior>
  <action>
    Add a `room.resolving` boolean race guard (D-09). In the `fire` handler, before calling `doShot(...)`: guard `if (room.resolving) return cb && cb({ ok:false, code:"BAD_STATE" });`, then set `room.resolving = true;`, call `doShot`, and clear `room.resolving = false;` immediately after (wrap in try/finally so an exception cannot leave the flag stuck). Apply the same set/clear around the `doShot` call inside the `useAbility` `scatter` branch (server.js ~line 802), since it also resolves shots.

    At the top of `onTurnTimeout(room, who)` (server.js line 468), add `if (room.resolving) return;` so a timeout that fires mid-resolution is skipped rather than double-resolving the turn. Initialize `resolving: false` in the `createRoom` room object (server.js line 576) alongside `turnTimer: null` so the field always exists.

    Reuse the existing `BAD_STATE` code convention (it is also introduced in plan 01-03 for doShot; using the same named code here is intentional and consistent).
  </action>
  <verify>
    <automated>npm test -- test/ratelimit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - A re-entrant fire while `room.resolving` is true returns `{ ok:false, code:"BAD_STATE" }` and doShot is not invoked twice (asserted in test).
    - onTurnTimeout returns early when `room.resolving` is true (asserted: turn/forfeit state unchanged).
    - `room.resolving` is set before and cleared after every doShot call site — `grep -c "resolving" server.js` returns ≥ 5 (init + fire set/clear + scatter set/clear + onTurnTimeout check).
    - The resolving flag is cleared in a finally (or equivalent) so a thrown doShot cannot leave it stuck true.
  </acceptance_criteria>
  <done>A simultaneous fire and turn-timeout can no longer both resolve the same shot; the race window (CONCERNS #7) is closed and proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client socket → fire/useAbility/chat handlers | Untrusted, possibly scripted, event stream crosses into game logic |
| turn timer ↔ in-flight fire | Concurrency boundary between the setTimeout callback and a socket event handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | fire/useAbility/chat handlers | mitigate | Per-player RateLimiterMemory (2/s, 1/s, 5/10s) drops floods with RATE_LIMITED; repeat abuse disconnects the socket (SEC-01, PITFALLS Pitfall 9). |
| T-02-07 | Tampering | turn-clock resolution | mitigate | `room.resolving` guard prevents simultaneous fire + timeout from both resolving a shot (D-09, CONCERNS #7) — closes the cheat that invalidates an opponent's shot. |
| T-02-R1 | Repudiation | rate-limit violations | accept | Account-level violation flagging deferred to Phase 2+ (no accounts yet, per CONTEXT deferred ideas); per-socket disconnect is the Phase-1 response. |
| T-02-SC | Tampering | npm install (`rate-limiter-flexible`) | mitigate | Task 0 blocking-human checkpoint verifies the package on npmjs.com before install. |
</threat_model>

<verification>
- `npm test -- test/ratelimit.test.js` passes.
- `grep -c "RateLimiterMemory" server.js` ≥ 3; limits match D-07 (2/1, 1/1, 5/10).
- `grep -n "RATE_LIMITED" server.js` appears in fire, useAbility, and chat handlers.
- `grep -c "resolving" server.js` ≥ 5; onTurnTimeout has an early-return on resolving.
</verification>

<success_criteria>
- fire and useAbility (and chat) are per-player rate-limited; rapid-fire yields RATE_LIMITED, not a crash (SEC-01).
- Repeated abuse disconnects the offending socket.
- The turn-clock race is guarded so fire + timeout cannot both resolve (D-09).
- Normal-rate play is unaffected by the limiters.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
