---
phase: 04-ranked-mode-leaderboard
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - elo.js
  - migrations/005_rankings.sql
  - test/elo.test.js
  - test/ranking.test.js
autonomous: true
requirements: [RANK-01, RANK-03, RANK-05]
must_haves:
  truths:
    - "elo.js updateRatings is a pure function with no DB/IO that reproduces the Glickman worked-example vector within tolerance"
    - "migrations/005_rankings.sql creates ratings, seasons, rating_history tables and ALTERs matches with rating snapshot columns"
    - "test/elo.test.js and test/ranking.test.js exist so downstream tasks have a RED-to-GREEN harness"
  artifacts:
    - path: "elo.js"
      provides: "Pure Glicko-2 single-game rating update (updateRatings)"
      exports: ["updateRatings"]
      min_lines: 40
    - path: "migrations/005_rankings.sql"
      provides: "ratings/seasons/rating_history tables + matches rating-snapshot ALTER"
      contains: "CREATE TABLE IF NOT EXISTS ratings"
    - path: "test/elo.test.js"
      provides: "Glicko-2 known-vector unit tests"
      contains: "1464"
    - path: "test/ranking.test.js"
      provides: "Wave-0 integration test scaffold for ratings write, leaderboard, season reset, guest block"
      contains: "005_rankings.sql"
  key_links:
    - from: "test/elo.test.js"
      to: "elo.js"
      via: "import updateRatings"
      pattern: "require\\(.*elo|from .*elo"
---

<objective>
Slice 1 of the ranked vertical: lay the pure-math foundation and the persistent schema that every later slice consumes, and create the Nyquist test scaffold (Wave 0) so all downstream tasks have a RED→GREEN harness.

This plan produces (a) `elo.js` — a pure, IO-free Glicko-2 single-game rating update validated against the canonical Glickman worked-example vector, and (b) `migrations/005_rankings.sql` — the `ratings`, `seasons`, `rating_history` tables plus the `matches` rating-snapshot ALTER. It also creates `test/elo.test.js` (unit, no DB) and `test/ranking.test.js` (the Wave-0 integration scaffold consumed by Plans 02–05).

Purpose: per the roadmap research flag, `elo.js` MUST be unit-tested against known Lichess/Glickman inputs BEFORE being wired to game-end (Plan 03). The schema is the storage substrate for RANK-01 (snapshot), RANK-03 (rd column for placement filter), and RANK-05 (history/seasons archive).
Output: `elo.js`, `migrations/005_rankings.sql`, `test/elo.test.js`, `test/ranking.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md
@.planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md
@.planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md
@.planning/phases/04-ranked-mode-leaderboard/04-VALIDATION.md
</context>

## Phase Goal

**As a** signed-in competitive player, **I want to** earn a Glicko-2 rating from ranked matches and see the top 100 players on a leaderboard, **so that** I have a reason to keep playing and climbing.

*(Reframed from the ROADMAP outcome-goal; the phase outcome is unchanged. This slice delivers the math + storage substrate the rest of the slices build on.)*

## Artifacts this phase produces (cumulative — this plan's contributions marked ◆)

- ◆ `elo.js` — pure Glicko-2; export `updateRatings(winner, loser)`
- ◆ `migrations/005_rankings.sql` — tables `ratings`, `seasons`, `rating_history`; ALTER `matches` add `winner_rating_before/after`, `loser_rating_before/after`
- ◆ `test/elo.test.js`, `test/ranking.test.js` (Wave-0 scaffold)
- (Plan 02) `ranked` room flag; error codes `RANKED_REQUIRES_ACCOUNT`, `RANKED_REQUIRES_CLASSIC`; ranked toggle UI + i18n keys
- (Plan 03) `recordMatch` 6th `ranked` param; rating UPSERT + matches snapshot in one transaction
- (Plan 04) `db.js` `getLeaderboard`/`refreshLeaderboardCache`; `store.js` `getLeaderboardCache`/`setLeaderboardCache`; `GET /api/leaderboard`; leaderboard UI + i18n keys
- (Plan 05) `scripts/season-reset.js`; `season-reset` npm script

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — elo.test.js (vectors) + ranking.test.js (integration stubs)</name>
  <files>test/elo.test.js, test/ranking.test.js</files>
  <read_first>
    - test/match.test.js (lines 1-65) — exact vitest file structure, fs/path/fileURLToPath header, static-DDL describe pattern, DB-gated integration pattern
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Standard Test Vectors section, lines ~520-550) — Glickman worked example r'≈1464.06 RD'≈151.52 σ'≈0.05999; period=1 sanity scenarios
    - .planning/phases/04-ranked-mode-leaderboard/04-VALIDATION.md (Per-Task Verification Map) — which requirement each test maps to
    - package.json — confirm "test": "vitest run" script and that vitest is installed
  </read_first>
  <behavior>
    - elo.test.js: importing updateRatings from ../elo and calling it with the Glickman 3-opponent reduction MUST be expressible; the gold assertion is the canonical worked example. Because elo.js takes exactly one winner + one loser (period=1), assert the single-opponent invariants the vector implies: equal-rated win raises winner toward ~1662 and lowers loser toward ~1338 (RD≈290); winner.rating > loser.rating after an equal-rated win; volatility stays ≈0.06 (within 1e-3); no NaN/Infinity in any output field.
    - elo.test.js MUST also include the canonical Glickman test using a 3-opponent helper OR a documented period=1 derivation that asserts r'≈1464.06 ± 0.5, RD'≈151.52 ± 0.5, σ'≈0.05999 ± 1e-4 once elo.js implements the multi-opponent path (mark this assertion as the authoritative correctness gate; if elo.js exposes only the 2-player API, assert the 2-player vectors from the research table within ±2 rating points as order-of-magnitude and leave a clearly-commented TODO to validate against pyglicko2).
    - ranking.test.js: static (no-DB) checks always run first — migrations/005_rankings.sql exists; file contains CREATE TABLE IF NOT EXISTS ratings, seasons, rating_history, and ALTER TABLE matches ... winner_rating_before. Server-source static greps: server.js contains RANKED_REQUIRES_ACCOUNT and RANKED_REQUIRES_CLASSIC. DB-gated integration describes are present but skipped when no DATABASE_URL/PGHOST (mirror match.test.js gating) — these become live in Plans 03/04/05.
  </behavior>
  <action>
    Create test/elo.test.js using the test/match.test.js header verbatim (vitest imports, __dirname via fileURLToPath, rootDir). Write the pure-math unit tests described in <behavior>. Constants under test: starting 1500/350/0.06, SCALE 173.7178, τ=0.5, ε=1e-6. These tests MUST be RED now (elo.js does not yet exist) — that is the intended Wave-0 state; Task 2 turns them GREEN.

    Create test/ranking.test.js with: a `describe("migrations/005_rankings.sql — static DDL", ...)` block asserting file existence and the CREATE/ALTER substrings; a `describe("server.js — ranked guards (static grep)", ...)` block grepping server.js source for the two error codes; and DB-gated `describe.skipIf(!hasDb)` blocks (stubs with TODO comments) for: same-transaction rating write (RANK-01), leaderboard provisional filter rd>=110 excluded (RANK-03), cache served-from-Redis (RANK-04), season reset archive-then-blend + idempotency (RANK-05). Use `const hasDb = !!(process.env.DATABASE_URL || process.env.PGHOST || process.env.PGDATABASE)` for gating. Do NOT inline implementation code blocks here — write real test files via the executor.
  </action>
  <verify>
    <automated>npx vitest run test/elo.test.js test/ranking.test.js 2>&1 | grep -Eiv '^#' | grep -Eq 'elo\.test|ranking\.test|FAIL|fail'</automated>
  </verify>
  <acceptance_criteria>
    - source: test/elo.test.js imports `updateRatings` from `../elo.js` (or `../elo`).
    - source: test/ranking.test.js contains the literal strings `005_rankings.sql`, `RANKED_REQUIRES_ACCOUNT`, `RANKED_REQUIRES_CLASSIC`.
    - behavior: `npx vitest run test/ranking.test.js` PASSES its static DDL/grep describes only once Task 2 (migration) + Plan 02 (guards) land; before that the static-DDL block legitimately reports missing-file/missing-grep — that is expected RED for Wave 0.
    - behavior: `npx vitest run test/elo.test.js` reports RED (cannot resolve ../elo) at the moment this task completes — confirming the harness is wired before elo.js exists.
    - No watch-mode flags anywhere; commands exit deterministically.
  </acceptance_criteria>
  <done>Both test files exist; elo.test.js is RED (no elo.js yet); ranking.test.js static blocks run and reference the three locked strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: elo.js — pure Glicko-2 single-game rating update (RANK-01 math)</name>
  <files>elo.js</files>
  <read_first>
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Pattern 1 elo.js skeleton lines ~202-310, Glicko-2 Algorithm Reference lines ~485-518, Don't Hand-Roll Illinois note, Pitfall 2 division-by-zero) — the algorithm spec is here; follow Glickman Step 5 exactly
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (elo.js section lines ~26-65) — module shape, flat-root placement, named-export + guard-clause conventions
    - store.js (lines 1-16, 64) — flat-root module shape + single module.exports object to mirror
    - test/elo.test.js — the assertions this file must satisfy (written in Task 1)
  </read_first>
  <behavior>
    - updateRatings({rating,rd,volatility}, {rating,rd,volatility}) returns {winner:{rating,rd,volatility}, loser:{rating,rd,volatility}} on Glicko display scale.
    - Reproduces the canonical Glickman worked example within tolerance (r'≈1464.06, RD'≈151.52, σ'≈0.05999) — this is the authoritative correctness gate.
    - Equal-rated win: winner rating rises, loser falls symmetrically, RD drops below 350; volatility ≈0.06.
    - No output field is NaN or Infinity for any input in the rating∈[1000,3000], RD∈[30,350] range (clamp E to [0.001,0.999] before computing variance — Pitfall 2).
    - Pure: no `require('./db')`, no `require('./store')`, no fs/network/Date side effects.
  </behavior>
  <action>
    Implement elo.js as a flat-root CommonJS module exporting `{ updateRatings }`. Constants: `SCALE = 173.7178`, `TAU = 0.5`, `EPS = 1e-6`, default 1500/350/0.06. Implement the Glicko-2 steps from 04-RESEARCH.md exactly: scale-down (μ=(r−1500)/SCALE, φ=RD/SCALE); `g(φ)=1/√(1+3φ²/π²)`; `E(μ,μj,φj)=1/(1+exp(−g(φj)(μ−μj)))` clamped to [0.001,0.999]; variance `v=1/(g²·E·(1−E))`; delta `Δ=v·g·(s−E)` with s=1 for winner, s=0 for loser; new volatility σ' via the Illinois bisection on `f(x)=exp(x)(Δ²−φ²−v−exp(x))/(2(φ²+v+exp(x))²)−(x−ln(σ²))/τ²`, iterating until `|B−A|<EPS`, then σ'=exp(A/2); pre-period φ*=√(φ²+σ'²); new φ'=1/√(1/φ*²+1/v); new μ'=μ+φ'²·g·(s−E); scale back r'=μ'·SCALE+1500, RD'=φ'·SCALE.

    The Illinois `newVolatility` MUST follow Glickman Step 5 exactly (the skeleton in 04-RESEARCH Pattern 1 is illustrative ONLY and has a buggy halving branch — do not copy it; implement the correct Illinois bracket selection: initialize A=ln(σ²), B per the conditional, then while |B−A|>EPS compute C=A+(A−B)f(A)/(f(B)−f(A)); if f(C)f(B)<0 set A=B,fA=fB else fA=fA/2; set B=C,fB=f(C)). Cross-check against github.com/ryankirkman/pyglicko2 mentally — output must give σ'≈0.05999 for the worked example. Guard-clause style, `[elo]` log prefix only if logging at all (prefer none — keep pure). No DB/IO imports.
  </action>
  <verify>
    <automated>npx vitest run test/elo.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test command: `npx vitest run test/elo.test.js` exits 0 (all green).
    - behavior: Glickman worked-example assertion passes within tolerance (r'≈1464.06±0.5, RD'≈151.52±0.5, σ'≈0.05999±1e-4).
    - source: elo.js exports exactly `{ updateRatings }` via `module.exports`; contains no `require("./db")`, no `require("./store")`, no `require("fs")`, no `require("redis")` (grep returns zero matches).
    - behavior: equal-rated win returns winner.rating > loser.rating and both rd < 350; no NaN in any field.
  </acceptance_criteria>
  <done>`npx vitest run test/elo.test.js` is fully GREEN; elo.js is pure (no DB/IO requires) and reproduces the canonical vector within tolerance.</done>
</task>

<task type="auto">
  <name>Task 3: migrations/005_rankings.sql — ratings, seasons, rating_history + matches ALTER</name>
  <files>migrations/005_rankings.sql</files>
  <read_first>
    - migrations/004_matches.sql (full) — header comment convention, IF NOT EXISTS pattern, IDX_ index naming, the anticipated Phase-4 ALTER note (lines 3-4), matches dedup UNIQUE(winner_id,loser_id,started_at)
    - migrations/001_identity.sql (lines 7-20) — users(id) SERIAL PK FK target, CREATE TABLE conventions
    - db.js (lines 42-66) — lexical migration runner; confirm `005_` is auto-picked after `004_` with no runner edit
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Schema: 005_rankings.sql section lines ~614-665) — recommended DDL; D-04/D-06/D-12 column shapes
    - .planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md (D-04, D-06, D-08, D-12) — locked column intent
  </read_first>
  <action>
    Create migrations/005_rankings.sql with a header comment (RANK-01/03/05 reference; "auto-applied by lexical runner after 004; all statements IF NOT EXISTS guarded").

    Per D-04: `CREATE TABLE IF NOT EXISTS ratings (user_id INTEGER PRIMARY KEY REFERENCES users(id), rating REAL NOT NULL DEFAULT 1500, rd REAL NOT NULL DEFAULT 350, volatility REAL NOT NULL DEFAULT 0.06, games_played INTEGER NOT NULL DEFAULT 0, updated_at TIMESTAMPTZ NOT NULL DEFAULT now())`. Add `CREATE INDEX IF NOT EXISTS IDX_ratings_rating_desc ON ratings (rating DESC)` with a comment that the leaderboard query (rating DESC WHERE rd<110, D-08/RANK-03) uses it.

    Per D-12: `CREATE TABLE IF NOT EXISTS seasons (id SERIAL PRIMARY KEY, label TEXT NOT NULL UNIQUE, started_at TIMESTAMPTZ NOT NULL DEFAULT now(), ended_at TIMESTAMPTZ)` — the UNIQUE(label) is the season-reset idempotency guard (Pitfall 5, RANK-05). `CREATE TABLE IF NOT EXISTS rating_history (id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL REFERENCES users(id), season_id INTEGER NOT NULL REFERENCES seasons(id), rating REAL NOT NULL, rd REAL NOT NULL, volatility REAL NOT NULL, games_played INTEGER NOT NULL, archived_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (user_id, season_id))` — UNIQUE(user_id,season_id) prevents double-archive (Pitfall 5). `CREATE INDEX IF NOT EXISTS IDX_rating_history_user_id ON rating_history (user_id)`.

    Per D-06: `ALTER TABLE matches ADD COLUMN IF NOT EXISTS winner_rating_before REAL, ADD COLUMN IF NOT EXISTS winner_rating_after REAL, ADD COLUMN IF NOT EXISTS loser_rating_before REAL, ADD COLUMN IF NOT EXISTS loser_rating_after REAL` (nullable — existing rows stay NULL; safe to re-run). Do not modify db.js (runner is lexical).
  </action>
  <verify>
    <automated>npx vitest run test/ranking.test.js -t "static DDL"</automated>
  </verify>
  <acceptance_criteria>
    - source: file contains `CREATE TABLE IF NOT EXISTS ratings`, `CREATE TABLE IF NOT EXISTS seasons`, `CREATE TABLE IF NOT EXISTS rating_history`, and `ALTER TABLE matches` with `winner_rating_before` … `loser_rating_after`.
    - source: `seasons` has `label TEXT NOT NULL UNIQUE`; `rating_history` has `UNIQUE (user_id, season_id)`.
    - source: every statement is `IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` guarded (re-runnable).
    - test command: `npx vitest run test/ranking.test.js -t "static DDL"` exits 0.
    - integration (deferred to a DB env): applying the migration twice via the runner is a no-op (no error) — re-run safety.
  </acceptance_criteria>
  <done>005_rankings.sql exists with the three tables + matches ALTER, all IF NOT EXISTS guarded; ranking.test.js static-DDL block is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Postgres | DDL applied at boot by the fail-loud runner; a malformed file aborts boot (DATA-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | elo.js silent wrong-volatility drift | mitigate | Unit-test against canonical Glickman vector (r'≈1464.06, RD'≈151.52, σ'≈0.05999); test must pass before Plan 03 wires it (research Don't Hand-Roll) |
| T-04-02 | Denial of Service | elo.js NaN/Infinity from extreme rating gap | mitigate | Clamp E to [0.001,0.999] before variance (Pitfall 2); test asserts no NaN/Infinity |
| T-04-03 | Tampering | season-reset double-archive | mitigate | Schema-level: UNIQUE(seasons.label) + UNIQUE(rating_history.user_id,season_id) created here; enforced at INSERT (Pitfall 5) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (research Package Legitimacy Audit: pg/redis/vitest already installed); nothing to install |
</threat_model>

<verification>
- `npx vitest run test/elo.test.js` is GREEN (pure-function vectors).
- `npx vitest run test/ranking.test.js -t "static DDL"` is GREEN.
- elo.js contains no DB/IO requires (grep).
- 005_rankings.sql is auto-discovered by the lexical runner (filename `005_` after `004_`).
</verification>

<success_criteria>
- elo.js reproduces the canonical Glicko-2 vector within tolerance and is pure.
- migrations/005_rankings.sql creates ratings/seasons/rating_history + matches snapshot columns, all re-runnable.
- The Wave-0 test scaffold (elo.test.js + ranking.test.js) exists and is wired for Plans 02–05.
</success_criteria>

<output>
Create `.planning/phases/04-ranked-mode-leaderboard/04-01-SUMMARY.md` when done.
</output>
