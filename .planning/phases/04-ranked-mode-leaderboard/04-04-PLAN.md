---
phase: 04-ranked-mode-leaderboard
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-03"]
files_modified:
  - store.js
  - db.js
  - server.js
  - public/app.jsx
  - test/ranking.test.js
autonomous: false
requirements: [RANK-03, RANK-04]
user_setup: []
must_haves:
  truths:
    - "GET /api/leaderboard returns the top 100 established players ordered by rating descending"
    - "Provisional players (rd >= 110) are excluded from the leaderboard but still rated normally"
    - "The leaderboard is served from a Redis cache with a <=5-minute TTL and is refreshed on every rating change"
    - "When Redis is unavailable the endpoint falls back to a direct Postgres query (graceful degrade)"
  artifacts:
    - path: "store.js"
      provides: "getLeaderboardCache / setLeaderboardCache Redis helpers"
      exports: ["getLeaderboardCache", "setLeaderboardCache"]
    - path: "db.js"
      provides: "getLeaderboard + refreshLeaderboardCache (rd<110 filter, top 100, post-commit refresh hook)"
      exports: ["getLeaderboard", "refreshLeaderboardCache"]
    - path: "server.js"
      provides: "GET /api/leaderboard endpoint"
      contains: "/api/leaderboard"
    - path: "public/app.jsx"
      provides: "leaderboard view + EN/VI strings"
      contains: "leaderboard"
  key_links:
    - from: "server.js GET /api/leaderboard"
      to: "db.js getLeaderboard"
      via: "await getLeaderboard()"
      pattern: "getLeaderboard"
    - from: "db.js recordMatch (post-commit)"
      to: "db.js refreshLeaderboardCache"
      via: "fire-and-forget after COMMIT"
      pattern: "refreshLeaderboardCache"
    - from: "public/app.jsx leaderboard view"
      to: "/api/leaderboard"
      via: "fetch"
      pattern: "fetch.*api/leaderboard"
---

<objective>
Slice 4 of the ranked vertical: the top 100 players become publicly visible. A `GET /api/leaderboard` endpoint returns the top 100 established players (rating DESC, D-10) filtered to `rd < 110` so provisional players are hidden until placement completes (RANK-03, D-08) — they are still rated normally, only their *visibility* is withheld. The board is served from a Redis cached-JSON key with a ≤5-minute TTL (RANK-04, D-09), refreshed fire-and-forget after every rating commit, and falls back to a direct Postgres query when Redis is down (store.js graceful-degrade). The lobby gains a leaderboard view (EN/VI).

After this plan, any visitor can open the leaderboard and see the top rated players; a brand-new ranked player does not appear until their RD drops below the threshold; ratings that change are reflected within 5 minutes.

Purpose: RANK-03 (placement gate) + RANK-04 (top-100 cache ≤5 min).
Output: store.js cache helpers; db.js getLeaderboard/refreshLeaderboardCache (rd<110, top 100) + post-commit refresh hook; GET /api/leaderboard; leaderboard UI + EN/VI strings; live integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md
@.planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md
@.planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md
</context>

## Phase Goal

**As a** signed-in competitive player, **I want to** earn a Glicko-2 rating from ranked matches and see the top 100 players on a leaderboard, **so that** I have a reason to keep playing and climbing.

## Artifacts this phase produces (this plan's contributions marked ◆)

- ◆ `store.js` exports `getLeaderboardCache()`, `setLeaderboardCache(json)` (Redis, TTL 300s)
- ◆ `db.js` exports `getLeaderboard()`, `refreshLeaderboardCache()` (rd<110 filter, top 100, JOIN users for display name/avatar)
- ◆ post-commit `refreshLeaderboardCache()` fire-and-forget hook in recordMatch's ranked branch
- ◆ `GET /api/leaderboard` Express endpoint (JSON, no auth, public non-sensitive fields)
- ◆ leaderboard view in app.jsx + i18n keys (`leaderboard.title`, `leaderboard.empty`, `leaderboard.provisionalNote`) EN/VI
- ◆ live integration tests (provisional filter, cache-hit, endpoint 200) in test/ranking.test.js
- (Plans 01-03) elo.js, migration, ranked flag/guards, rating write
- (Plan 05) season-reset CLI

<tasks>

<task type="auto">
  <name>Task 1: store.js leaderboard cache helpers + db.js getLeaderboard/refreshLeaderboardCache + recordMatch refresh hook (RANK-03/RANK-04, D-08/D-09/D-10)</name>
  <files>store.js, db.js, test/ranking.test.js</files>
  <read_first>
    - store.js (full, 1-64) — init/isEnabled/saveSnapshot/loadSnapshot graceful-degrade pattern, `ready` guard, KEY naming, the single module.exports object at line 64
    - db.js (lines 447-488) — recordMatch transaction + the ranked branch added in Plan 03 (post-COMMIT is where refreshLeaderboardCache fires); lines 532-545 module.exports flat list
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Pattern 3 Redis Leaderboard Cache lines ~357-420; Pitfall 3 filter direction rd<110=show; Pitfall 6 Redis client exposure; Open Question 2 add exports to store.js)
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (store.js additions lines ~184-240; db.js additions lines ~117-180; Redis Graceful-Degrade Pattern)
    - migrations/005_rankings.sql — ratings columns + IDX_ratings_rating_desc; users.display_name/avatar_url columns (from 002_accounts.sql)
  </read_first>
  <action>
    In store.js, add two exports following the saveSnapshot/loadSnapshot shape: `setLeaderboardCache(json)` — guard `if (!ready) return;` then `await client.set("battleship:leaderboard", json, { EX: 300 })` in a try/catch that logs `[store]` and swallows; `getLeaderboardCache()` — guard `if (!ready) return null;` then `await client.get("battleship:leaderboard")` returning the raw string or null, try/catch returns null on error. Add a `const LEADERBOARD_KEY = "battleship:leaderboard"` and `const LEADERBOARD_TTL = 300` near the existing KEY. Append both names to the module.exports object. Keep Redis client encapsulated in store.js (Pitfall 6 — never expose `client`).

    In db.js, add `buildLeaderboard(client)` (private): parameterized SELECT joining ratings→users `WHERE r.rd < 110 ORDER BY r.rating DESC LIMIT 100`, returning id, display_name, avatar_url, rating, rd, games_played. Comment the filter: "rd < 110 = established = shown; rd >= 110 = provisional = hidden (Pitfall 3)". Add `getLeaderboard()`: `require('./store')` lazily; if `getLeaderboardCache()` returns a cached string, JSON.parse and return it; on miss, run buildLeaderboard against a pooled client, `setLeaderboardCache(JSON.stringify(rows))`, return rows; wrap cache reads so a Redis error falls through to the direct Postgres query. Add `refreshLeaderboardCache()`: build top-100 from Postgres, setLeaderboardCache, return rows; `[leaderboard]`-log + swallow on error. Export both `getLeaderboard` and `refreshLeaderboardCache`.

    Wire the refresh hook: in recordMatch, AFTER `COMMIT` succeeds and only in the ranked branch, fire-and-forget `refreshLeaderboardCache().catch(() => {})` (never before COMMIT, never awaited — Pitfall 1/4).

    In test/ranking.test.js, activate the DB-gated RANK-03/RANK-04 tests: (a) a player with rd >= 110 is excluded from getLeaderboard, one with rd < 110 included, ordered by rating DESC; (b) getLeaderboard second call returns cached JSON (assert via a Redis-present run, or assert setLeaderboardCache was called); (c) getLeaderboard returns an array even when Redis is unavailable (fallback path).
  </action>
  <verify>
    <automated>npx vitest run test/ranking.test.js -t "leaderboard"</automated>
  </verify>
  <acceptance_criteria>
    - source: store.js module.exports includes `getLeaderboardCache` and `setLeaderboardCache`; both guard on `ready` and never expose `client`.
    - source: db.js module.exports includes `getLeaderboard` and `refreshLeaderboardCache`; the leaderboard SELECT contains `rd < 110`, `ORDER BY` rating `DESC`, and `LIMIT 100`.
    - source: recordMatch calls `refreshLeaderboardCache()` only AFTER COMMIT, fire-and-forget, in the ranked branch (grep: refreshLeaderboardCache appears after the COMMIT line).
    - behavior (DB): a player with rd>=110 is absent from getLeaderboard; one with rd<110 present; order is rating DESC.
    - behavior: getLeaderboard returns an array when Redis is disabled (direct-query fallback).
    - test command: `npx vitest run test/ranking.test.js -t "leaderboard"` exits 0.
  </acceptance_criteria>
  <done>store.js + db.js leaderboard helpers exist with rd<110 top-100 filter and Redis-then-Postgres fallback; recordMatch refreshes the cache post-commit; leaderboard tests green.</done>
</task>

<task type="auto">
  <name>Task 2: server.js GET /api/leaderboard endpoint (RANK-04)</name>
  <files>server.js, test/ranking.test.js</files>
  <read_first>
    - server.js (lines 54-58) — /healthz and /metrics endpoint shape (JSON, no auth) to mirror; the require of db.js helpers at the top of server.js
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (Express endpoint pattern lines ~300-316) — `app.get("/api/leaderboard", async ...)` with try/catch → 500 LEADERBOARD_UNAVAILABLE
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Security Domain — leaderboard DDoS mitigated by cache-first; only non-sensitive public fields)
    - db.js — getLeaderboard export (Task 1)
  </read_first>
  <action>
    Add `app.get("/api/leaderboard", async (req, res) => { ... })` near /healthz and /metrics (54-58), mirroring their JSON/no-auth shape. Import `getLeaderboard` from db.js (extend the existing destructured require). In the handler, `try { const rows = await getLeaderboard(); res.json(rows); } catch (e) { console.error("[leaderboard] endpoint error:", e.message); res.status(500).json({ error: "LEADERBOARD_UNAVAILABLE" }); }`. Return ONLY the non-sensitive public fields buildLeaderboard already selects (id, display_name, avatar_url, rating, rd, games_played) — no emails, no internal ids beyond the public user id. No rate-limit middleware needed (cache-first absorbs load), but place it on the same router as the other GET endpoints so existing CSP/middleware apply.

    In test/ranking.test.js, activate the endpoint integration test (DB-gated): start/import the express app (or hit the route handler) and assert `GET /api/leaderboard` returns 200 with a JSON array.
  </action>
  <verify>
    <automated>npx vitest run test/ranking.test.js -t "endpoint" && node --check server.js</automated>
  </verify>
  <acceptance_criteria>
    - source: server.js contains `app.get("/api/leaderboard"` and calls `getLeaderboard()`.
    - source: error path responds `500` with `{ error: "LEADERBOARD_UNAVAILABLE" }`; success path `res.json(rows)`.
    - source: no sensitive fields (email, password_hash) appear in the leaderboard select/response.
    - behavior (DB): GET /api/leaderboard returns 200 and a JSON array.
    - regression: `node --check server.js` exits 0.
  </acceptance_criteria>
  <done>GET /api/leaderboard serves the cached top-100 as JSON (public fields only), 500s gracefully on failure; endpoint test green; server.js parses.</done>
</task>

<task type="auto">
  <name>Task 3: public/app.jsx leaderboard view + EN/VI strings (RANK-04 UI)</name>
  <files>public/app.jsx</files>
  <read_first>
    - public/app.jsx (lines 697-720) — lobby layout + screen-switching patterns; where a "Leaderboard" entry point fits
    - public/app.jsx (lines 26-27, 134-135) — EN/VI i18n string pairs to mirror
    - public/app.jsx — existing `setScreen` / screen-state pattern and any existing fetch usage for /api/* (e.g. /api/profile from Phase 2) to mirror the fetch + render shape
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (app.jsx component section lines ~361-429) — useState, i18n, fetch-and-render analog (profile view from Phase 2)
  </read_first>
  <action>
    Add a leaderboard screen/view: an entry point (button/link) in the lobby that switches to a leaderboard screen, which on mount `fetch("/api/leaderboard")` and renders a ranked list (rank #, display_name, avatar, rating; round rating to integer for display). Reuse the Phase-2 profile-view fetch+render pattern (same `fetch` → state → map shape). Show an empty state when the array is empty (`t("leaderboard.empty")`) and a short note that provisional players are not yet listed (`t("leaderboard.provisionalNote")`). Display names are already HTML-escaped server-side (sanitizeDisplayName) — render as text, do not dangerouslySetInnerHTML.

    Add EN/VI i18n pairs: `leaderboard.title` ("Leaderboard" / "Bảng xếp hạng"), `leaderboard.empty` ("No ranked players yet" / "Chưa có người chơi xếp hạng"), `leaderboard.provisionalNote` ("New players appear after placement matches" / "Người chơi mới xuất hiện sau các trận định hạng"), `leaderboard.rating` ("Rating" / "Điểm"). Additive only — preserve existing strings.
  </action>
  <verify>
    <automated>npm run build:game && node -e "const s=require('fs').readFileSync('public/app.jsx','utf8'); const need=['/api/leaderboard','leaderboard.title','leaderboard.empty']; const miss=need.filter(n=>!s.includes(n)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('leaderboard UI wiring ok')"</automated>
  </verify>
  <acceptance_criteria>
    - source: app.jsx fetches `/api/leaderboard` and renders the returned rows; references `leaderboard.title` and `leaderboard.empty`.
    - source: EN and VI variants of `leaderboard.title` / `leaderboard.empty` / `leaderboard.provisionalNote` exist.
    - source: display names rendered as text (no dangerouslySetInnerHTML on leaderboard rows).
    - build: `npm run build:game` succeeds.
  </acceptance_criteria>
  <done>Leaderboard view fetches and renders the top 100 with empty + provisional-note states, EN/VI strings added, esbuild builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — leaderboard view + provisional gating (EN/VI)</name>
  <what-built>
    The GET /api/leaderboard endpoint (Redis-cached top 100, rd<110 filter), the post-commit cache refresh, and the leaderboard UI with empty/provisional states. Automated above; this checkpoint confirms the rendered board, the placement gate, and i18n.
  </what-built>
  <how-to-verify>
    1. Build + start the server with Postgres + Redis reachable.
    2. Play (or seed) a couple of ranked matches between two signed-in accounts so ratings exist.
    3. Open the Leaderboard view: top players appear ordered by rating, with name + avatar + integer rating. Confirm the VI strings by switching language.
    4. Confirm a brand-new ranked account (few games, rd still >= 110) does NOT appear in the list, while the provisional note is shown.
    5. Optional: stop Redis and reload the leaderboard — it still loads (direct Postgres fallback), confirming graceful degrade.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., provisional player visible, wrong order, missing VI string, fallback broken).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → GET /api/leaderboard | Unauthenticated public reads cross into the rating store (via cache) |
| db.js → Redis | Cached leaderboard JSON crosses to/from an optional external store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure | Leaderboard leaks sensitive fields (email, hashes) | mitigate | buildLeaderboard SELECTs only id, display_name, avatar_url, rating, rd, games_played; no email/credential columns joined |
| T-04-13 | Denial of Service | Hammering /api/leaderboard overloads Postgres | mitigate | Cache-first: requests within the 300s TTL hit Redis only; Postgres touched at most once per TTL window per refresh (research Security Domain) |
| T-04-14 | Tampering | Provisional filter inverted → new accounts shown / established hidden | mitigate | `WHERE rd < 110` with an explicit comment (Pitfall 3); integration test asserts rd>=110 excluded |
| T-04-15 | Information Disclosure | Stored-XSS via display_name on the public board | mitigate | display_name sanitized/HTML-escaped at write (db.js sanitizeDisplayName); rendered as text, no dangerouslySetInnerHTML |
| T-04-SC | Tampering | npm installs | accept | No new packages (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run test/ranking.test.js -t "leaderboard"` and `-t "endpoint"` GREEN.
- `npm run build:game` + `node --check server.js` succeed.
- Human checkpoint confirms board render, provisional gate, EN/VI, and Redis-down fallback.
</verification>

<success_criteria>
- GET /api/leaderboard returns the top 100 established players (rating DESC, rd<110) from a Redis cache with ≤5-min TTL refreshed on rating change (RANK-04); provisional players excluded but still rated (RANK-03); graceful Postgres fallback when Redis is down.
</success_criteria>

<output>
Create `.planning/phases/04-ranked-mode-leaderboard/04-04-SUMMARY.md` when done.
</output>
