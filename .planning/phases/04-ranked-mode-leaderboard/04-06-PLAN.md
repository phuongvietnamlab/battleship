---
phase: 04-ranked-mode-leaderboard
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - server.js
  - test/ranking.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RANK-01, RANK-02]
user_setup: []
must_haves:
  truths:
    - "A ranked game in progress during a server restart (REDIS_URL set) survives the Redis snapshot round-trip with room.ranked, room.recorded, and every seat userId intact, so recordMatch still writes the match row and rating update when that game ends."
    - "A serialize -> restore round-trip preserves ranked===true, recorded, and userId on rooms restored from a snapshot."
    - "REQUIREMENTS.md traceability shows RANK-02 as Complete (server guards, lobby toggle, EN/VI i18n are implemented and human-approved)."
  artifacts:
    - path: "server.js"
      provides: "serializeRooms persists ranked/recorded/userId; restoreRooms restores them; both functions exported via TEST_EXPORTS for round-trip testing"
      contains: "userId"
    - path: "test/ranking.test.js"
      provides: "CR-01 serialize->restore round-trip assertion (no DB required)"
      contains: "serializeRooms"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RANK-02 status flipped Pending -> Complete"
      contains: "RANK-02"
  key_links:
    - from: "server.js serializeRooms"
      to: "server.js restoreRooms"
      via: "Redis snapshot round-trip (ranked/recorded/userId fields)"
      pattern: "userId: p.userId"
    - from: "server.js restoreRooms output"
      to: "db.js recordMatch"
      via: "room.ranked + room.players[id].userId read at end-of-game paths"
      pattern: "room\\.ranked"
---

<objective>
Close the single Phase-4 blocker (CR-01) and the RANK-02 tracking discrepancy surfaced by 04-VERIFICATION.md.

CR-01 (BLOCKER, RANK-01 data loss): `serializeRooms` (server.js ~781-817) builds the Redis snapshot but omits `room.ranked`, `room.recorded`, and per-seat `userId`. `restoreRooms` (server.js ~822-867) rebuilds rooms without those fields. When REDIS_URL is set and the process restarts mid-ranked-game, the restored room has `ranked === undefined` and every seat `userId === null/undefined`. When that game ends, every recordMatch call site (server.js disconnect ~774, forfeit ~1099, normal-win ~1173, leave ~1590) passes `wId=null`/`lId=null`, so recordMatch's `winnerId == null || loserId == null` guard skips BOTH the match write AND the rating write. Crash-recovery silently fails on exactly the games (ranked) where correctness matters most. Fix: persist and restore the four fields, and add a round-trip test proving they survive.

RANK-02 (documentation only): REQUIREMENTS.md marks RANK-02 "Pending" (line ~126) but the server guards (createRoom ~1236, joinRoom ~1292), lobby ranked toggle, and EN/VI i18n are fully implemented and human-approved per 04-02-SUMMARY. Flip the traceability row to "Complete".

Purpose: RANK-01 (ranked rating + match write must survive the documented Redis crash-recovery path) and RANK-02 (correct status tracking).
Output: serializeRooms/restoreRooms persist+restore ranked/recorded/userId; serializeRooms+restoreRooms exported via TEST_EXPORTS; a no-DB round-trip test in test/ranking.test.js; REQUIREMENTS.md RANK-02 = Complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md
@.planning/phases/04-ranked-mode-leaderboard/04-VERIFICATION.md
@.planning/phases/04-ranked-mode-leaderboard/04-REVIEW.md
</context>

## Phase Goal

**As a** signed-in competitive player, **I want to** earn a Glicko-2 rating from ranked matches and see the top 100 players on a leaderboard, **so that** I have a reason to keep playing and climbing.

## Already-satisfied human checks (not re-run by this plan)

- Ranked guest-block UI (EN/VI) — approved in 04-02-SUMMARY (Plan 02 Task 3).
- Leaderboard provisional gating (rd>=110 hidden) — approved in 04-04-SUMMARY (Plan 04 Task 4).
- Season-reset CLI on live DB — approved in 04-05-SUMMARY (Plan 05 Task 3).
These are recorded approved; this gap-closure plan does NOT create tasks to re-run them.

## Artifacts this phase produces (this plan's contributions marked ◆)

- ◆ `server.js` serializeRooms: per-room `ranked: !!r.ranked`, `recorded: !!r.recorded`; per-player `userId: p.userId ?? null`
- ◆ `server.js` restoreRooms: per-room `ranked: !!s.ranked`, `recorded: !!s.recorded`; per-player `userId: p.userId ?? null`
- ◆ `server.js` TEST_EXPORTS additions: `serializeRooms`, `restoreRooms` (enables the round-trip test)
- ◆ `test/ranking.test.js` CR-01 round-trip describe block (no DB required)
- ◆ `.planning/REQUIREMENTS.md` RANK-02 row: Pending -> Complete
- (Plans 01-05) elo.js, migration, ranked flag/guards, rating write, leaderboard, season-reset (unchanged)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist + restore ranked/recorded/userId in the Redis snapshot path (CR-01, RANK-01) and add a no-DB round-trip test</name>
  <files>server.js, test/ranking.test.js</files>
  <read_first>
    - server.js (lines 779-817) — current serializeRooms body: per-player object built at 788-798 (ready/occ/hits/ships/inv/bonus/skipNext/timeouts/profile — NO userId); per-room object built at 802-814 (code/order/started/startedAt/turn/scores/lastStarter/mode/powerups/mines/players — NO ranked, NO recorded). Confirm exact field list and line numbers before editing — do not trust these blindly.
    - server.js (lines 819-867) — current restoreRooms body: per-player rebuild at 830-843 (NO userId); per-room rebuild at 847-861 (NO ranked, NO recorded).
    - server.js (lines 749-774, 1094-1099, 1169-1173, 1582-1590) — the four recordMatch call sites that read `room.players[id]?.userId ?? null` and `room.ranked`/`room.recorded`; this confirms the exact field names the snapshot must round-trip (userId on the player object, ranked + recorded on the room object).
    - server.js (lines 1683-1696) — current module.exports.TEST_EXPORTS object (doShot, rooms, sweepRooms, escapeHtml, sanitizeProfile, sanitizeChat, cspMiddleware, CSP_HEADER_VALUE, app). serializeRooms/restoreRooms are NOT exported yet.
    - test/ranking.test.js (lines 1-12, 110-139) — vitest import style, the `hasDb` gate pattern, and the static-grep / describe block conventions to mirror. The round-trip test is pure in-memory (operates on the rooms map) and MUST run without a DB (do not put it behind the hasDb gate).
    - .planning/phases/04-ranked-mode-leaderboard/04-REVIEW.md (CR-01 section, lines 52-86) — the exact four-field fix.
  </read_first>
  <action>
    In serializeRooms (server.js): add `userId: p.userId ?? null` to the per-player serialized object (alongside the existing profile field, ~line 797). Add `ranked: !!r.ranked` and `recorded: !!r.recorded` to the per-room serialized object (alongside mode/powerups, ~line 810). Keep the boolean coercion (`!!`) and `?? null` exactly as the review specifies so undefined never serializes to a non-JSON value.

    In restoreRooms (server.js): add `userId: p.userId ?? null` to the rebuilt per-player object (alongside profile, ~line 842). Add `ranked: !!s.ranked` and `recorded: !!s.recorded` to the rebuilt per-room object (alongside mode/powerups, ~line 856). Read from the snapshot variable `s` (room) / `p` (player) to match the existing restore code's naming.

    Add `serializeRooms` and `restoreRooms` to the `TEST_EXPORTS` object in module.exports (~line 1683) so the round-trip test can import them. Do not change any other export; additive only.

    In test/ranking.test.js, add a NEW describe block "CR-01: snapshot round-trip preserves ranked/recorded/userId (no DB)". It imports `TEST_EXPORTS` from ../server (mirror how other server-importing tests load it; this test must NOT be gated on hasDb). The test: (1) clears the live `rooms` map, (2) constructs a started two-seat ranked room directly in `rooms` with `ranked: true`, `recorded: false`, two players each having a distinct `userId` (e.g. 101, 202) plus the minimal required seat fields (ready/occ/hits/ships/inv/bonus/skipNext/timeouts/order), (3) calls `serializeRooms()` and asserts the output room object has `ranked === true`, `recorded === false`, and each serialized player object has the expected `userId`, (4) clears `rooms`, calls `restoreRooms(snapshot)`, and asserts the restored room has `ranked === true`, `recorded === false`, and each restored player has the original `userId`. Because restoreRooms arms timers/grace-release on restore, either restore into a controlled state and clear `rooms` afterward, or assert the fields immediately after restore before any timer fires; keep the test deterministic and self-cleaning (clear `rooms` in afterAll/afterEach so it does not leak into other suites).

    Also add a focused static-grep assertion (mirrors the existing Plan-03/04 grep tests) that serializeRooms source contains `userId: p.userId` and the per-room serialize contains `ranked:` and `recorded:`, and restoreRooms contains the same — a cheap regression guard that survives even if the in-memory test is later refactored.
  </action>
  <verify>
    <automated>npx vitest run test/ranking.test.js -t "CR-01" && node --check server.js</automated>
  </verify>
  <acceptance_criteria>
    - source: serializeRooms per-player serialized object contains key `userId` (value `p.userId ?? null`); per-room serialized object contains keys `ranked` (`!!r.ranked`) and `recorded` (`!!r.recorded`).
    - source: restoreRooms rebuilt per-player object contains key `userId` (value `p.userId ?? null`); rebuilt per-room object contains keys `ranked` (`!!s.ranked`) and `recorded` (`!!s.recorded`).
    - source: module.exports.TEST_EXPORTS now includes `serializeRooms` and `restoreRooms`; no existing export removed.
    - behavior (no DB): the round-trip test builds a ranked room with two seats (userId 101/202, ranked:true, recorded:false), serializes, restores, and asserts ranked===true, recorded===false, and both seat userId values are preserved.
    - regression: `node --check server.js` exits 0; `npx vitest run test/ranking.test.js` shows no previously-passing test newly failing.
    - test command: `npx vitest run test/ranking.test.js -t "CR-01"` exits 0.
  </acceptance_criteria>
  <done>serializeRooms and restoreRooms persist+restore ranked, recorded, and per-seat userId; both functions are test-exported; a no-DB round-trip test proves a ranked game survives serialize->restore with userId/ranked/recorded intact; server.js parses; no regression in the existing ranking suite.</done>
</task>

<task type="auto">
  <name>Task 2: Flip RANK-02 traceability status Pending -> Complete (documentation, RANK-02)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 123-138) — the traceability table; the RANK-02 row at ~line 126 currently reads `| RANK-02 | Phase 4 — Ranked Mode & Leaderboard | Pending |`. Confirm the exact row text and surrounding rows before editing.
    - .planning/phases/04-ranked-mode-leaderboard/04-VERIFICATION.md (Requirements Coverage table + "RANK-02 tracking discrepancy" note, lines 120-128, 175-177) — the rationale: server guards (createRoom 1236, joinRoom 1292), lobby toggle, EN/VI i18n implemented and human-approved.
    - .planning/phases/04-ranked-mode-leaderboard/04-02-SUMMARY.md — confirm RANK-02 implementation + human-verify approval recorded there before flipping status.
  </read_first>
  <action>
    Edit only the RANK-02 row of the traceability table in .planning/REQUIREMENTS.md, changing the status cell from `Pending` to `Complete`. Do not alter any other row, the coverage counts, or surrounding prose. This is the documentation fix for the verified tracking discrepancy — the implementation already exists and is human-approved; only the status cell was stale.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); const m=s.match(/\| *RANK-02 *\|[^\n]*\|/); if(!m){console.error('RANK-02 row not found');process.exit(1)} if(!/Complete/.test(m[0])||/Pending/.test(m[0])){console.error('RANK-02 not marked Complete:',m[0]);process.exit(1)} console.log('RANK-02 = Complete')"</automated>
  </verify>
  <acceptance_criteria>
    - source: the RANK-02 row in .planning/REQUIREMENTS.md shows status `Complete` and no longer contains `Pending`.
    - source: no other requirement row's status changed; coverage counts unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS.md RANK-02 traceability row reads Complete; no other rows or counts touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory rooms map <-> Redis snapshot | Game/identity state (ranked, recorded, seat userId) crosses to/from the optional Redis store on snapshot save and restart restore |
| restored room state -> db.js recordMatch | Identity (userId) and ranked flag flow from restored state into the rating + match write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | Restored ranked room missing userId/ranked causes silent loss of the rating + match write (data integrity failure on crash-recovery) | mitigate | serializeRooms/restoreRooms persist+restore `ranked`, `recorded`, `userId`; round-trip test asserts they survive (CR-01) |
| T-04-17 | Repudiation | A completed ranked result produces no match row after a mid-game restart, so the outcome cannot be reconstructed | mitigate | Same fix — recordMatch now receives real wId/lId/ranked after restore, so the match row + rating are written and auditable |
| T-04-18 | Tampering | A non-boolean/undefined `ranked`/`recorded` or a non-JSON `userId` corrupts the serialized snapshot | mitigate | `!!` boolean coercion and `?? null` on each field guarantees JSON-safe, deterministic values (matches existing serialize idioms) |
| T-04-SC | Tampering | npm installs | accept | No new packages added in this plan (no install task) |
</threat_model>

<verification>
- `npx vitest run test/ranking.test.js -t "CR-01"` GREEN (round-trip preserves ranked/recorded/userId, no DB needed).
- `node --check server.js` exits 0; full `npx vitest run test/ranking.test.js` shows no regression.
- REQUIREMENTS.md RANK-02 row reads Complete (grep gate above).
- Already-approved human checks (guest-block UI, provisional gating, season-reset CLI) are recorded approved in SUMMARYs — not re-run here.
</verification>

<success_criteria>
- A ranked game restored from a Redis snapshot retains room.ranked, room.recorded, and every seat userId, so recordMatch writes the match row and rating update when the game ends (RANK-01 crash-recovery path closed; CR-01 resolved).
- A no-DB serialize->restore round-trip test proves the three fields survive.
- REQUIREMENTS.md marks RANK-02 Complete (RANK-02 tracking discrepancy resolved).
</success_criteria>

<output>
Create `.planning/phases/04-ranked-mode-leaderboard/04-06-SUMMARY.md` when done.
</output>
