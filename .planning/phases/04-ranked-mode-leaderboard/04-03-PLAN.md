---
phase: 04-ranked-mode-leaderboard
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - db.js
  - server.js
  - test/ranking.test.js
autonomous: true
requirements: [RANK-01]
user_setup: []
must_haves:
  truths:
    - "After a ranked match ends, winner and loser ratings are updated in the SAME transaction as the matches row"
    - "An unranked game (room.ranked false, or either seat a guest) writes NO rating change"
    - "Each ranked match writes winner/loser rating_before/after onto the matches row"
    - "A rating-write failure rolls back the match row too, and never blocks the gameOver screen"
  artifacts:
    - path: "db.js"
      provides: "recordMatch extended with 6th ranked param + in-transaction Glicko-2 write via elo.js"
      contains: "updateRatings"
    - path: "server.js"
      provides: "all four recordMatch call sites pass room.ranked as 6th arg"
      contains: "room.ranked"
  key_links:
    - from: "db.js recordMatch"
      to: "elo.js updateRatings"
      via: "require + call inside BEGIN/COMMIT"
      pattern: "updateRatings"
    - from: "server.js recordMatch call sites"
      to: "db.js recordMatch ranked param"
      via: "6th positional arg room.ranked"
      pattern: "recordMatch\\([^)]*ranked"
---

<objective>
Slice 3 of the ranked vertical: a completed ranked match now actually changes ratings — atomically, in the same DB transaction as the match record (RANK-01, success criterion 1, D-07). `recordMatch` gains a 6th `ranked` parameter; when `ranked === true` AND both user ids are non-null integers (D-03), it reads each player's current rating (defaulting to 1500/350/0.06), calls the pure `elo.js updateRatings`, UPSERTs both `ratings` rows, and stamps `winner_rating_before/after` + `loser_rating_before/after` onto the `matches` row (D-06) — all inside the existing `BEGIN/COMMIT` block. All four `recordMatch` call sites in server.js pass `room.ranked`.

After this plan, two signed-in players finishing a ranked room see their ratings move and the match row carries the rating snapshot. Unranked games (or any game with a guest seat) write no rating change. A DB failure rolls everything back and still never blocks the end screen (inherits Phase-3 D-07 best-effort contract).

Purpose: RANK-01 (rating updates in the same transaction as the match record).
Output: extended `recordMatch` in db.js (in-transaction rating write); 6th-arg `room.ranked` at all four call sites in server.js; live DB integration tests in test/ranking.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md
@.planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md
@.planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md
@.planning/phases/03-match-recording/03-CONTEXT.md
</context>

## Phase Goal

**As a** signed-in competitive player, **I want to** earn a Glicko-2 rating from ranked matches and see the top 100 players on a leaderboard, **so that** I have a reason to keep playing and climbing.

## Artifacts this phase produces (this plan's contributions marked ◆)

- ◆ `recordMatch(winnerId, loserId, reason, mode, startedAt, ranked)` — 6th param added (default false)
- ◆ in-transaction rating read + `updateRatings` call + `ratings` UPSERT (both players) + `matches` snapshot UPDATE
- ◆ `room.ranked` passed at all four call sites (server.js ~1086 forfeit, ~1160 normal win, ~761 disconnect-grace, ~1570 leave)
- ◆ live DB integration tests in test/ranking.test.js (RANK-01 atomicity)
- (Plan 01) `elo.js`, `migrations/005_rankings.sql`
- (Plan 02) `room.ranked` flag, guards
- (Plan 04) leaderboard endpoint + cache + UI; post-commit `refreshLeaderboardCache()` hook
- (Plan 05) season-reset CLI

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: db.js — recordMatch 6th ranked param + same-transaction Glicko-2 rating write + matches snapshot (RANK-01, D-03/D-06/D-07)</name>
  <files>db.js, test/ranking.test.js</files>
  <read_first>
    - db.js (lines 447-488) — recordMatch in full: graceful no-op guard, VALID_REASONS, unresolvable-seat guard, the BEGIN → INSERT matches → COMMIT block (472-488), the swallow-never-rethrow catch (D-07)
    - db.js (lines 532-545) — module.exports flat list (add nothing new here unless exporting a helper; the rating write lives INSIDE recordMatch)
    - elo.js — `updateRatings(winner, loser)` signature + return shape (created in Plan 01)
    - .planning/phases/04-ranked-mode-leaderboard/04-RESEARCH.md (Pattern 2 Same-Transaction Rating Write lines ~312-355; Open Question 3 — add `ranked=false` as 6th positional param; Pitfall 1 transaction scope; Pitfall 2 NaN reject)
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (db.js additions section lines ~117-180; Transaction Pattern; Parameterized SQL; logging prefix)
    - migrations/005_rankings.sql — ratings columns + matches snapshot columns (created in Plan 01)
    - test/match.test.js — DB-gating pattern for integration tests
  </read_first>
  <behavior>
    - recordMatch signature becomes `recordMatch(winnerId, loserId, reason, mode, startedAt, ranked = false)`.
    - When `ranked === true` and both ids are non-null: inside the existing transaction, after the matches INSERT and before COMMIT, read both players' current ratings (SELECT rating, rd, volatility, games_played FROM ratings WHERE user_id=$1; default 1500/350/0.06/0 if no row), call `updateRatings`, UPSERT both ratings rows (INSERT … ON CONFLICT (user_id) DO UPDATE, games_played+1), then UPDATE the matches row's four rating snapshot columns matching on (winner_id, loser_id, started_at).
    - When `ranked` is false OR either id is null: NO ratings/snapshot queries run — behaves exactly as Phase-3 recordMatch (no regression; existing match.test.js stays green).
    - Atomicity (RANK-01): if any rating query throws (e.g. NaN rejected by REAL column), the whole transaction ROLLs back — the matches row is NOT committed — and the error is swallowed (D-07), never rethrown, never blocking gameOver.
    - The rating write uses the SAME `client` as the matches INSERT (never `pool`, never a second connection) — Pitfall 1.
  </behavior>
  <action>
    Extend recordMatch in db.js per 04-RESEARCH Pattern 2. Add `ranked = false` as the 6th positional parameter. `require('./elo')` at the top of db.js alongside the other requires. Inside the `try` block, after the `INSERT INTO matches` line and before `COMMIT`, add a conditional block `if (ranked && winnerId != null && loserId != null) { … }` containing: two parameterized SELECTs against `ratings` (default object 1500/350/0.06/0 when no row); a call to `updateRatings(wBefore, lBefore)`; two parameterized UPSERTs (`INSERT INTO ratings (user_id, rating, rd, volatility, games_played, updated_at) VALUES ($1,$2,$3,$4,$5, now()) ON CONFLICT (user_id) DO UPDATE SET rating=$2, rd=$3, volatility=$4, games_played=$5, updated_at=now()`, incrementing games_played); one parameterized UPDATE stamping the four matches snapshot columns matched on winner_id/loser_id/started_at. All values bound `$N` (never concatenated). Keep the existing swallow-catch (ROLLBACK + `console.error("[match] …")` + no rethrow) — the rating write rides the same catch, so a failure rolls back the match too (RANK-01 atomicity). Do NOT touch the graceful no-op guard, VALID_REASONS, or unresolvable-seat guard. Add a `[match]` (or `[rating]`) log line on the rating branch for grep-ability.

    In test/ranking.test.js, turn the Wave-0 DB-gated RANK-01 stubs into live tests (run only when DATABASE_URL/PGHOST set): (a) ranked recordMatch updates both ratings rows AND the matches snapshot columns in one row; (b) unranked recordMatch leaves ratings untouched; (c) a forced failure (e.g. NaN rating, or an unresolvable user) leaves NEITHER a ratings change NOR a matches row (atomic rollback). Use real test user rows (insert into users first) and clean up after.
  </action>
  <verify>
    <automated>npx vitest run test/ranking.test.js test/match.test.js</automated>
  </verify>
  <acceptance_criteria>
    - source: db.js recordMatch signature includes a 6th `ranked` parameter (default false) and `require("./elo")` is present.
    - source: the rating SELECT/UPSERT/matches-UPDATE all use the same `client` variable as the matches INSERT (grep: no `pool.connect` second time, no `pool.query` inside the rating branch).
    - behavior (DB): ranked recordMatch with two real users updates both ratings rows and the matches snapshot columns; rating values are finite (no NaN).
    - behavior (DB): unranked recordMatch writes a matches row but NO ratings rows.
    - behavior (DB): a rating-write failure rolls back the matches row (atomicity) — assert zero matching matches rows after the failed call.
    - regression: `npx vitest run test/match.test.js` (Phase-3 contract) stays fully green.
    - test command: `npx vitest run test/ranking.test.js test/match.test.js` exits 0.
  </acceptance_criteria>
  <done>recordMatch writes Glicko-2 rating updates + matches snapshot atomically inside its existing transaction when ranked, no-ops when unranked, rolls back on failure, never rethrows; ranking + match tests green.</done>
</task>

<task type="auto">
  <name>Task 2: server.js — pass room.ranked as 6th arg at all four recordMatch call sites (RANK-01 wiring)</name>
  <files>server.js</files>
  <read_first>
    - server.js (lines 1082-1087) — forfeit/endGameForfeit recordMatch call
    - server.js (lines 1156-1161) — doShot normal-win recordMatch call
    - server.js (lines 730-762) — scheduleSeatRelease disconnect-grace: the `disconnectRecord` object (736-745) and the recordMatch call at 761
    - server.js (lines 1556-1572) — leaveRoom recordMatch call (1570)
    - .planning/phases/04-ranked-mode-leaderboard/04-PATTERNS.md (recordMatch call pattern lines ~287-298; Fire-and-Forget section lines ~513-521) — add room.ranked as 6th arg, keep `.catch(() => {})`
    - .planning/phases/04-ranked-mode-leaderboard/04-CONTEXT.md (D-03: only ranked when BOTH seats signed in — note db.js already guards on non-null ids, so passing room.ranked is sufficient; a guest seat yields a null userId and the rating branch is skipped)
  </read_first>
  <action>
    Add `room.ranked` as the 6th positional argument to all four `recordMatch(...)` calls, preserving the existing `.catch(() => {})` and dedup guards:
    1. Forfeit (~1086): `recordMatch(wId, lId, reason, room.mode, room.startedAt, room.ranked).catch(() => {})`.
    2. Normal win (~1160): `recordMatch(wId, lId, "normal", room.mode, room.startedAt, room.ranked).catch(() => {})`.
    3. Disconnect-grace (~761): the call reads from `disconnectRecord`; add `ranked` to the captured `disconnectRecord` object (736-745) as `ranked: r2.ranked` and pass `disconnectRecord.ranked` as the 6th arg at 761.
    4. Leave (~1570): `recordMatch(wId, lId, "leave", room.mode, room.startedAt, room.ranked).catch(() => {})`.
    Change nothing about the `!room.recorded` dedup, `order.length === 2`, or emit ordering (gameOver before recordMatch stays — Pitfall 4). D-03 is enforced downstream: a guest seat has `userId: null`, so db.js skips the rating branch even when room.ranked is true.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('server.js','utf8'); const m=s.match(/recordMatch\([^)]*\)/g)||[]; const ranked=m.filter(c=>/ranked/.test(c)); if(ranked.length<4){console.error('Only',ranked.length,'recordMatch calls pass ranked; expected >=4');process.exit(1)} console.log('all 4 recordMatch sites pass ranked')"</automated>
  </verify>
  <acceptance_criteria>
    - source: grep finds at least four `recordMatch(...)` invocations that include `ranked` (room.ranked or disconnectRecord.ranked).
    - source: the `disconnectRecord` object literal (736-745) includes a `ranked` field sourced from `r2.ranked`.
    - source: every call retains `.catch(() => {})` and the gameOver-before-recordMatch ordering is unchanged.
    - regression: server.js still parses (node -c style) — `node --check server.js` exits 0.
  </acceptance_criteria>
  <done>All four recordMatch call sites forward room.ranked; disconnectRecord carries ranked; dedup/emit ordering unchanged; server.js parses clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game-end path → Postgres (ratings + matches) | Server-computed rating values cross into durable storage inside one transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Rating updated but no match row (or vice versa) — inconsistent ledger | mitigate | All writes share one `client`/`BEGIN`/`COMMIT`; failure ROLLs back both (RANK-01, Pitfall 1); integration test asserts atomic rollback |
| T-04-09 | Tampering | Self-play / guest inflates rating | mitigate | Rating branch runs only when both `userId` are non-null integers (D-03); guest seats are null → skipped. Distinct-account self-play throttling is a Phase-5 matchmaking concern (accepted here) |
| T-04-10 | Denial of Service | NaN/Infinity rating silently written | mitigate | elo.js clamps E (Pitfall 2); REAL column rejects NaN → transaction rolls back via the swallow path; no partial write |
| T-04-11 | Denial of Service | Slow/unavailable DB hangs the end screen | mitigate | gameOver emitted before recordMatch; recordMatch is fire-and-forget `.catch(()=>{})`, never awaited on the client path (D-07, Pitfall 4) |
| T-04-SC | Tampering | npm installs | accept | No new packages (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run test/ranking.test.js test/match.test.js` GREEN (atomicity + no Phase-3 regression).
- `node --check server.js` exits 0; grep confirms 4 recordMatch sites pass ranked.
- Rating branch uses one shared client (no second pool connection).
</verification>

<success_criteria>
- Ranked match ratings + matches snapshot are written atomically in one transaction (RANK-01); unranked games write no rating change; failures roll back fully and never block gameOver.
</success_criteria>

<output>
Create `.planning/phases/04-ranked-mode-leaderboard/04-03-SUMMARY.md` when done.
</output>
